==========================================
Generating (v2): Web Application Firewall (WAF)
==========================================

[Pass 1/4] Generating initial design...

OpenAI Codex v0.72.0 (research preview)
--------
workdir: /Users/dominikdabrowski/pr/system-design-atlas
model: gpt-5.2
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: high
reasoning summaries: auto
session id: 019b2af7-2c9d-7502-b390-6f2c005cc6d9
--------
user
# System Design Document Generator (v2)

You are a **seasoned senior architect** who has built and operated large-scale systems. Your job is to produce system design documents that are **insightful and elegant**, not just thorough.

## Core Philosophy

**A great design:**
- Identifies the 1-2 genuinely hard subproblems and solves them elegantly
- Uses boring, proven technology for everything else
- Makes clear, defensible trade-off decisions
- Teaches something non-obvious
- Is simple enough that a small team could build and operate it

**A weak design:**
- Lists components without explaining why
- Over-engineers with custom solutions when standard tools work fine
- Hedges on trade-offs instead of taking positions
- Is comprehensive but doesn't teach anything new

## Output Format

```markdown
---
title: "{title}"
category: "{category}"
difficulty: "{difficulty}"
tags: [...]
---

## Overview

[2-3 paragraphs: What is this system? What's the key insight that makes this design elegant?]

## What Makes This Hard

[What do naive implementations get wrong? What's the trap that catches most teams?]

## Requirements

### Functional Requirements
[Focus on the non-obvious requirements that drive architectural decisions.]

### Scale Targets
[Concrete numbers with reasoning. Why these numbers matter.]

## Key Design Decisions

[The 2-3 architectural choices that define this system. For each:]
- What we chose
- What we rejected
- Why

## Architecture

```mermaid
[Simple diagram - max 8 nodes. Elegance over completeness.]
```

### Components

[For each component, explain its purpose and why it earns its place in the design.]

## Deep Dive: [The Hardest Part]

[Pick the most interesting/difficult aspect and go deep. Show your reasoning.]

## Trade-offs

[Be explicit about what this design optimizes for and what it sacrifices.]

| Optimized For | Sacrificed |
|--------------|------------|
| ... | ... |

## Failure Modes

[The 2-3 failure scenarios that matter most. For each: what happens, how you detect it, how you recover.]

## What I'd Do Differently At...

- **10x scale:** [What changes?]
- **100x scale:** [What breaks and needs rearchitecting?]

## Operational Notes

[The non-obvious things an on-call engineer needs to know.]
```

## Writing Guidelines

### Be Opinionated
- **Bad:** "You could use Postgres or MySQL or MongoDB depending on your needs"
- **Good:** "Use Postgres. Its JSONB and strong consistency make it the right choice here."

### Explain the Why
- **Bad:** "Use a message queue for decoupling"
- **Good:** "The queue absorbs traffic spikes during flash sales, letting us size the order service for average load, not peak"

### Go Deep on Hard Parts
- **Bad:** "Implement leader election using a consensus algorithm"
- **Good:** [3 paragraphs explaining the specific approach, edge cases, and failure modes]

### Stay Practical
- **Bad:** "Implement a custom distributed consensus protocol"
- **Good:** "Use etcd for leader election—it's battle-tested and one less thing to debug at 3am"

## Mermaid Guidelines

1. **Max 6-8 nodes** - elegance means knowing what to leave out
2. **Short labels** (2-4 words)
3. **One level of subgraphs** max
4. **Quote special characters**: `A["Load Balancer (L7)"]`

## Anti-Patterns

1. **Component soup** - listing every possible service without justification
2. **Hedge words** - "could", "might", "optionally", "depending on requirements"
3. **Textbook regurgitation** - explaining what Kafka/Redis/K8s does (assume the reader knows)
4. **Future-proofing** - solving problems you don't have yet
5. **Complexity theater** - making things complicated to seem thorough

---

## Problem to Solve

**Title**: Web Application Firewall (WAF)
**Category**: Security & Access Control

**Problem Statement**:
Design an edge security layer to filter SQL injection, XSS, and malicious payloads at line speed.

Generate the system design document now.
mcp startup: no servers
warning: Repository snapshot encountered large untracked directories: grades (201 files). This can slow Codex; consider adding these paths to .gitignore or disabling undo in your config.


thinking
**Designing a WAF System**

I want to create a system design document that outlines an opinionated design for a web application firewall (WAF) using managed solutions like Cloudflare or AWS WAF and CDN. My main goal is to strike a balance between false positives and negatives while ensuring line speed. It’s important to run at the edge, and I’m considering using a streaming HTTP parser with custom filters. Overall, I’m leaning towards building on Envoy with minimal state for effective rate limiting, avoiding complexity with eBPF/XDP.
thinking
**Crafting the WAF Documentation**

I need to detail the internal control plane for managing rules, compiling them into DFAs, and distributing to Points of Presence (POPs) through a CDN. A big challenge will be rule management and latency while ensuring safe updates. It's essential to include requirements, scalability numbers, and an architecture diagram showing around six to eight nodes. I'll dive deep into rule evaluation at line speed, handling false positives, and operational notes like shadow mode and safe deployment. I'll be concise, using YAML front matter and avoiding hedge words.
codex
```markdown
---
title: "Web Application Firewall (WAF)"
category: "Security & Access Control"
difficulty: "Hard"
tags: [security, edge, waf, http, ddos, rules, observability]
---

## Overview

This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck. The key insight is to treat the WAF as a **deterministic, precompiled packet of logic** running in the data plane, and push all complexity—rule authoring, tuning, rollout safety, and analytics—into a separate control plane.

Most WAF attempts fail by mixing the two: they run heavy parsing and dynamic policy decisions inline, then “fix” latency with caching and exceptions until the WAF becomes a patchwork. This design keeps the data plane boring: strict resource caps, streaming parsing, bounded-time matching, and a small set of actions (allow/block/challenge/log).

## What Makes This Hard

Naive implementations get trapped in a three-way trade-off:

1. **Accuracy vs. latency:** Deep inspection and complex regex rules catch more attacks but explode CPU and tail latency under load.
2. **False positives vs. safety:** Over-blocking breaks real users; under-blocking leaves gaps. Teams end up shipping rules blindly because they lack feedback loops.
3. **Operational safety at the edge:** A bad ruleset rollout can take down *every* property globally faster than any application deploy.

The hard part is not “detect SQLi/XSS”; it’s **doing it deterministically under adversarial input** (regex bombs, giant headers, chunked bodies) while maintaining **safe, observable rule evolution**.

## Requirements

### Functional Requirements
- Detect and block common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
- Support **actions**: `ALLOW`, `BLOCK`, `CHALLENGE` (e.g., CAPTCHA/JS), `LOG_ONLY`.
- Support **per-route policy** (e.g., stricter on `/login`, looser on `/search`) without per-request DB lookups.
- Provide **shadow mode** and explainability: for a blocked request, identify rule(s) and matched locations.
- Provide **safe rollout**: canary, staged POP rollout, instant rollback.

### Scale Targets
- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP. This is the regime where tail latency and CPU spikes define user experience.
- **Latency budget:** +2 ms p99 added by WAF at the edge. Anything higher becomes noticeable and forces teams to bypass protection.
- **Payload limits:** Up to 32 KB headers, 1 MB body (configurable per route). Hard caps prevent adversarial “slow parse” attacks.
- **Ruleset size:** 10k–50k signatures + heuristics. Large enough to be useful; small enough to keep matching predictable.

## Key Design Decisions

- **Choose: deterministic data plane with precompiled rules**
  - Rejected: dynamic rule evaluation via database calls or scripting per request
  - Why: bounded execution time is the only reliable defense under adversarial input.

- **Choose: streaming HTTP parsing + bounded body inspection**
  - Rejected: buffering entire requests for “full” inspection
  - Why: buffering creates a memory DoS vector and increases latency under chunked uploads.

- **Choose: control-plane-driven rollouts with shadow + canary**
  - Rejected: “push rules everywhere” or manual POP changes
  - Why: rules are code; unsafe rollout is a global outage waiting to happen.

## Architecture

```mermaid
flowchart LR
  C[Client] --> E[Edge Proxy]
  E --> W[WAF Filter]
  W --> O[Origin App]
  W --> L[Event Stream]
  CP[Rule Control Plane] --> RD[Rule Distributor]
  RD --> W
  L --> A[Analytics/Tuning]
```

### Components

- **Edge Proxy**
  - Terminates TLS, normalizes HTTP (canonicalization), and enforces hard limits (header size, URL length, body size, timeouts). This is where many attacks become cheap to stop.

- **WAF Filter (Data Plane)**
  - Runs inline in the proxy request path.
  - Implements: request normalization, signature matching, anomaly scoring, and action enforcement.
  - Uses only local memory and preloaded rule artifacts; no network calls.

- **Origin App**
  - Receives already-scrubbed traffic. The WAF is additive security, not a substitute for secure coding.

- **Event Stream**
  - Asynchronous security telemetry (sampled for allows, full for blocks/challenges) for tuning and incident response.

- **Rule Control Plane**
  - UI/API for rule authoring, exemptions, per-route policy, and rollout workflows.
  - Produces a signed, versioned ruleset artifact.

- **Rule Distributor**
  - Distributes artifacts to POPs, supports staged rollout, health checks, and instant rollback by version pinning.

- **Analytics/Tuning**
  - Aggregates matches, false-positive reports, and origin error correlations to drive rule improvements.

## Deep Dive: Line-Speed Detection Without Regex DoS

The core engineering problem is: **match lots of attack patterns on hostile input with a strict time budget**.

### 1) Canonicalization first, or rules are useless
Attackers win when the WAF and the origin parse differently. The data plane performs a strict normalization pass:
- Percent-decoding with limits (reject over-decode depth)
- Path normalization (`/a/../b` → `/b`) with strict RFC behavior
- Header and cookie parsing with bounded field counts and sizes
- UTF-8 validation; invalid encodings become immediate block signals

This makes matching predictable and reduces “bypass by encoding”.

### 2) Two-tier matching: cheap first, expensive rarely
All rules are compiled into bounded-time primitives:
- **Aho–Corasick** (or similar) for large sets of fixed substrings (fast multi-pattern scan).
- **RE2-style regex** only (no catastrophic backtracking). Regex is reserved for cases that genuinely need it, and is capped by input length.
- **Anomaly scoring**: individual weak signals add up; one shaky match doesn’t block by itself.

The filter runs:
1. **Cheap scans** on URL + headers + cookies (almost always enough for common attacks).
2. **Conditional body inspection** only for routes/content-types that accept user input and only up to a configured byte budget (e.g., first 64 KB of decoded fields).

### 3) Streaming body inspection with hard caps
The WAF never needs the full body to catch most real attacks, but it must not be blind:
- For `application/x-www-form-urlencoded` and `application/json`, parse incrementally and scan decoded values.
- Stop inspecting after the route’s inspection budget; continue forwarding the stream.
- If a match requires bytes beyond the budget, that’s a deliberate trade-off: the system favors availability and predictable latency.

### 4) Rollout safety is part of the detection design
Every rule is shipped with:
- **Mode** (`LOG_ONLY` → `BLOCK`)
- **Scope** (routes, methods, content-types)
- **Confidence** (used for anomaly scoring)
Rules move through a workflow: shadow → canary POPs → regional → global, with automated guardrails (block-rate deltas, origin error deltas). This is how you avoid “a ruleset update broke checkout worldwide”.

## Trade-offs

| Optimized For | Sacrificed |
|--------------|------------|
| Predictable latency under attack | Full deep-packet inspection of huge bodies |
| Safe, fast rollouts and rollback | “One-off” custom logic in the request path |
| Low operational complexity in data plane | Some detection sophistication moves to tuning/analytics |
| Consistent parsing across edge | Perfect fidelity to every origin framework quirk |

## Failure Modes

- **Bad ruleset blocks legitimate traffic**
  - What happens: sudden spikes in `BLOCK` on critical routes (login/checkout).
  - Detect: block-rate anomaly + correlated origin revenue/errors + user reports.
  - Recover: instant rollback by ruleset version pin; keep per-route emergency bypass token that downgrades to `LOG_ONLY`.

- **CPU saturation at POP during attack (tail latency blowup)**
  - What happens: proxy queues grow, p99 latency climbs, timeouts.
  - Detect: WAF CPU per request, queue depth, p99 per-route, dropped connections.
  - Recover: shed expensive work first (disable body inspection on low-risk routes), enforce stricter limits, increase challenges, then scale out.

- **Parsing differential causes bypass**
  - What happens: edge allows traffic that the origin interprets differently.
  - Detect: exploit reports, WAF/origin mismatch signatures, unusual 4xx/5xx patterns.
  - Recover: patch canonicalization, add regression tests with real framework parsers, ship fix as a high-priority ruleset + binary rollout.

## What I'd Do Differently At...

- **10x scale:**
  - Add POP-local reputation caches (hot IPs, JA3/JA4 fingerprints) and tighter adaptive challenges to cut load before deep inspection.
  - Increase automated tuning: promote rules from shadow to block based on measured false-positive rates.

- **100x scale:**
  - Split the WAF into two layers: ultra-cheap L7 gate (limits, reputation, bot/challenge) and a selective deep inspection tier for only suspicious traffic.
  - Invest in formal parser compatibility testing across common origin stacks; bypass-by-parsing becomes the dominant class of failures.

## Operational Notes

- Shadow mode is non-negotiable: run new rules as `LOG_ONLY` first and require block-rate and origin-error gates before promotion.
- Maintain strict per-route inspection budgets; the fastest WAF is the one that refuses to do unbounded work.
- Log with privacy in mind: store structured match metadata, not raw payloads, and sample allows aggressively.
- Keep an emergency “break-glass” config path that can downgrade to `ALLOW + LOG` globally when the control plane is impaired.
```
tokens used
2,742
```markdown
---
title: "Web Application Firewall (WAF)"
category: "Security & Access Control"
difficulty: "Hard"
tags: [security, edge, waf, http, ddos, rules, observability]
---

## Overview

This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck. The key insight is to treat the WAF as a **deterministic, precompiled packet of logic** running in the data plane, and push all complexity—rule authoring, tuning, rollout safety, and analytics—into a separate control plane.

Most WAF attempts fail by mixing the two: they run heavy parsing and dynamic policy decisions inline, then “fix” latency with caching and exceptions until the WAF becomes a patchwork. This design keeps the data plane boring: strict resource caps, streaming parsing, bounded-time matching, and a small set of actions (allow/block/challenge/log).

## What Makes This Hard

Naive implementations get trapped in a three-way trade-off:

1. **Accuracy vs. latency:** Deep inspection and complex regex rules catch more attacks but explode CPU and tail latency under load.
2. **False positives vs. safety:** Over-blocking breaks real users; under-blocking leaves gaps. Teams end up shipping rules blindly because they lack feedback loops.
3. **Operational safety at the edge:** A bad ruleset rollout can take down *every* property globally faster than any application deploy.

The hard part is not “detect SQLi/XSS”; it’s **doing it deterministically under adversarial input** (regex bombs, giant headers, chunked bodies) while maintaining **safe, observable rule evolution**.

## Requirements

### Functional Requirements
- Detect and block common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
- Support **actions**: `ALLOW`, `BLOCK`, `CHALLENGE` (e.g., CAPTCHA/JS), `LOG_ONLY`.
- Support **per-route policy** (e.g., stricter on `/login`, looser on `/search`) without per-request DB lookups.
- Provide **shadow mode** and explainability: for a blocked request, identify rule(s) and matched locations.
- Provide **safe rollout**: canary, staged POP rollout, instant rollback.

### Scale Targets
- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP. This is the regime where tail latency and CPU spikes define user experience.
- **Latency budget:** +2 ms p99 added by WAF at the edge. Anything higher becomes noticeable and forces teams to bypass protection.
- **Payload limits:** Up to 32 KB headers, 1 MB body (configurable per route). Hard caps prevent adversarial “slow parse” attacks.
- **Ruleset size:** 10k–50k signatures + heuristics. Large enough to be useful; small enough to keep matching predictable.

## Key Design Decisions

- **Choose: deterministic data plane with precompiled rules**
  - Rejected: dynamic rule evaluation via database calls or scripting per request
  - Why: bounded execution time is the only reliable defense under adversarial input.

- **Choose: streaming HTTP parsing + bounded body inspection**
  - Rejected: buffering entire requests for “full” inspection
  - Why: buffering creates a memory DoS vector and increases latency under chunked uploads.

- **Choose: control-plane-driven rollouts with shadow + canary**
  - Rejected: “push rules everywhere” or manual POP changes
  - Why: rules are code; unsafe rollout is a global outage waiting to happen.

## Architecture

```mermaid
flowchart LR
  C[Client] --> E[Edge Proxy]
  E --> W[WAF Filter]
  W --> O[Origin App]
  W --> L[Event Stream]
  CP[Rule Control Plane] --> RD[Rule Distributor]
  RD --> W
  L --> A[Analytics/Tuning]
```

### Components

- **Edge Proxy**
  - Terminates TLS, normalizes HTTP (canonicalization), and enforces hard limits (header size, URL length, body size, timeouts). This is where many attacks become cheap to stop.

- **WAF Filter (Data Plane)**
  - Runs inline in the proxy request path.
  - Implements: request normalization, signature matching, anomaly scoring, and action enforcement.
  - Uses only local memory and preloaded rule artifacts; no network calls.

- **Origin App**
  - Receives already-scrubbed traffic. The WAF is additive security, not a substitute for secure coding.

- **Event Stream**
  - Asynchronous security telemetry (sampled for allows, full for blocks/challenges) for tuning and incident response.

- **Rule Control Plane**
  - UI/API for rule authoring, exemptions, per-route policy, and rollout workflows.
  - Produces a signed, versioned ruleset artifact.

- **Rule Distributor**
  - Distributes artifacts to POPs, supports staged rollout, health checks, and instant rollback by version pinning.

- **Analytics/Tuning**
  - Aggregates matches, false-positive reports, and origin error correlations to drive rule improvements.

## Deep Dive: Line-Speed Detection Without Regex DoS

The core engineering problem is: **match lots of attack patterns on hostile input with a strict time budget**.

### 1) Canonicalization first, or rules are useless
Attackers win when the WAF and the origin parse differently. The data plane performs a strict normalization pass:
- Percent-decoding with limits (reject over-decode depth)
- Path normalization (`/a/../b` → `/b`) with strict RFC behavior
- Header and cookie parsing with bounded field counts and sizes
- UTF-8 validation; invalid encodings become immediate block signals

This makes matching predictable and reduces “bypass by encoding”.

### 2) Two-tier matching: cheap first, expensive rarely
All rules are compiled into bounded-time primitives:
- **Aho–Corasick** (or similar) for large sets of fixed substrings (fast multi-pattern scan).
- **RE2-style regex** only (no catastrophic backtracking). Regex is reserved for cases that genuinely need it, and is capped by input length.
- **Anomaly scoring**: individual weak signals add up; one shaky match doesn’t block by itself.

The filter runs:
1. **Cheap scans** on URL + headers + cookies (almost always enough for common attacks).
2. **Conditional body inspection** only for routes/content-types that accept user input and only up to a configured byte budget (e.g., first 64 KB of decoded fields).

### 3) Streaming body inspection with hard caps
The WAF never needs the full body to catch most real attacks, but it must not be blind:
- For `application/x-www-form-urlencoded` and `application/json`, parse incrementally and scan decoded values.
- Stop inspecting after the route’s inspection budget; continue forwarding the stream.
- If a match requires bytes beyond the budget, that’s a deliberate trade-off: the system favors availability and predictable latency.

### 4) Rollout safety is part of the detection design
Every rule is shipped with:
- **Mode** (`LOG_ONLY` → `BLOCK`)
- **Scope** (routes, methods, content-types)
- **Confidence** (used for anomaly scoring)
Rules move through a workflow: shadow → canary POPs → regional → global, with automated guardrails (block-rate deltas, origin error deltas). This is how you avoid “a ruleset update broke checkout worldwide”.

## Trade-offs

| Optimized For | Sacrificed |
|--------------|------------|
| Predictable latency under attack | Full deep-packet inspection of huge bodies |
| Safe, fast rollouts and rollback | “One-off” custom logic in the request path |
| Low operational complexity in data plane | Some detection sophistication moves to tuning/analytics |
| Consistent parsing across edge | Perfect fidelity to every origin framework quirk |

## Failure Modes

- **Bad ruleset blocks legitimate traffic**
  - What happens: sudden spikes in `BLOCK` on critical routes (login/checkout).
  - Detect: block-rate anomaly + correlated origin revenue/errors + user reports.
  - Recover: instant rollback by ruleset version pin; keep per-route emergency bypass token that downgrades to `LOG_ONLY`.

- **CPU saturation at POP during attack (tail latency blowup)**
  - What happens: proxy queues grow, p99 latency climbs, timeouts.
  - Detect: WAF CPU per request, queue depth, p99 per-route, dropped connections.
  - Recover: shed expensive work first (disable body inspection on low-risk routes), enforce stricter limits, increase challenges, then scale out.

- **Parsing differential causes bypass**
  - What happens: edge allows traffic that the origin interprets differently.
  - Detect: exploit reports, WAF/origin mismatch signatures, unusual 4xx/5xx patterns.
  - Recover: patch canonicalization, add regression tests with real framework parsers, ship fix as a high-priority ruleset + binary rollout.

## What I'd Do Differently At...

- **10x scale:**
  - Add POP-local reputation caches (hot IPs, JA3/JA4 fingerprints) and tighter adaptive challenges to cut load before deep inspection.
  - Increase automated tuning: promote rules from shadow to block based on measured false-positive rates.

- **100x scale:**
  - Split the WAF into two layers: ultra-cheap L7 gate (limits, reputation, bot/challenge) and a selective deep inspection tier for only suspicious traffic.
  - Invest in formal parser compatibility testing across common origin stacks; bypass-by-parsing becomes the dominant class of failures.

## Operational Notes

- Shadow mode is non-negotiable: run new rules as `LOG_ONLY` first and require block-rate and origin-error gates before promotion.
- Maintain strict per-route inspection budgets; the fastest WAF is the one that refuses to do unbounded work.
- Log with privacy in mind: store structured match metadata, not raw payloads, and sample allows aggressively.
- Keep an emergency “break-glass” config path that can downgrade to `ALLOW + LOG` globally when the control plane is impaired.
```
[Pass 1/4] Draft saved to: /Users/dominikdabrowski/pr/system-design-atlas/drafts-v2/web-application-firewall.md

[Pass 2/4] Challenging the design...

OpenAI Codex v0.72.0 (research preview)
--------
workdir: /Users/dominikdabrowski/pr/system-design-atlas
model: gpt-5.2
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: high
reasoning summaries: auto
session id: 019b2af8-5fbe-7c43-9e92-6d476cc22eb5
--------
user
# System Design Challenger (v2) - The Constructive Critic

You are a **constructive critic** reviewing a system design. Your goal is to make it more **elegant**, not tear it down.

## Your Mindset

You're the senior engineer who reviews designs before they go to architecture review. You want the design to succeed, so you:
- Find simpler alternatives the author might have missed
- Stress-test the failure modes
- Ensure every component earns its place
- Push for clarity and honesty in trade-offs

## Your Questions

### "Is There a Simpler Path?"
For each custom component, consider proven alternatives:
- Could **Postgres** handle this with JSONB/LISTEN-NOTIFY/advisory locks?
- Could **Redis** solve this with its built-in data structures?
- Could **etcd/ZooKeeper** handle the coordination needs?
- Could **Kafka/SQS** replace a custom queue?
- Could **S3** replace custom storage?
- Could an **existing open-source project** (Temporal, Litmus, etc.) be used instead?

The goal isn't to reject custom solutions—it's to ensure they're justified.

### "What Happens When...?"
Stress-test the design:
- What happens when the database is down for 5 minutes?
- What happens when there's a network partition between services?
- What happens when one component is slow but not failing?
- What happens when you deploy a bad config?
- What happens when traffic 10x's unexpectedly?
- What happens when an engineer makes a mistake at 3am?

### "What's the Elegant Core?"
Identify what makes this design special:
- What's the key insight that makes this design work?
- Which 1-2 components are genuinely custom and interesting?
- What boring parts could be simplified to highlight the interesting parts?

### "Can a Small Team Own This?"
Challenge operational complexity:
- How many services does on-call need to understand?
- What's the deploy story? Can one person deploy this safely?
- Is the complexity budget appropriate for the team size?

## Output Format

Produce a structured critique:

```markdown
## Elegance Check

### The Core Insight
[What's the one thing this design does that's genuinely clever or non-obvious?]

### Components That Earn Their Place
| Component | Why It's Necessary |
|-----------|-------------------|
| ... | ... |

### Simplification Opportunities
| Current | Simpler Alternative | Trade-off |
|---------|---------------------|-----------|
| ... | ... | ... |

## Stress Test

### Failure Scenarios
For each, note whether the design addresses it and how:

1. **[Scenario]**
   - Design's answer: [What the doc says, or "not addressed"]
   - Recommendation: [Strengthen / Acceptable / Overkill]

[3-5 key scenarios]

## Recommendations

### Must Fix
[Issues that would cause problems in production]

### Should Consider
[Improvements that would make the design more elegant]

### Nice to Have
[Polish items for the final version]

## What's Working Well
[Be specific about what the design gets right—good designs should be praised]
```

## Remember

Your job is to make the design **better**, not to prove you're smarter than the author. A good critique:

1. Acknowledges what's working
2. Finds simpler paths without dismissing thoughtful choices
3. Ensures failure modes are covered
4. Results in a more elegant final design

The measure of success: the author thinks "that's a better way to do it" not "this reviewer doesn't understand what I'm building."

---

## Design to Challenge

```markdown
---
title: "Web Application Firewall (WAF)"
category: "Security & Access Control"
difficulty: "Hard"
tags: [security, edge, waf, http, ddos, rules, observability]
---

## Overview

This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck. The key insight is to treat the WAF as a **deterministic, precompiled packet of logic** running in the data plane, and push all complexity—rule authoring, tuning, rollout safety, and analytics—into a separate control plane.

Most WAF attempts fail by mixing the two: they run heavy parsing and dynamic policy decisions inline, then “fix” latency with caching and exceptions until the WAF becomes a patchwork. This design keeps the data plane boring: strict resource caps, streaming parsing, bounded-time matching, and a small set of actions (allow/block/challenge/log).

## What Makes This Hard

Naive implementations get trapped in a three-way trade-off:

1. **Accuracy vs. latency:** Deep inspection and complex regex rules catch more attacks but explode CPU and tail latency under load.
2. **False positives vs. safety:** Over-blocking breaks real users; under-blocking leaves gaps. Teams end up shipping rules blindly because they lack feedback loops.
3. **Operational safety at the edge:** A bad ruleset rollout can take down *every* property globally faster than any application deploy.

The hard part is not “detect SQLi/XSS”; it’s **doing it deterministically under adversarial input** (regex bombs, giant headers, chunked bodies) while maintaining **safe, observable rule evolution**.

## Requirements

### Functional Requirements
- Detect and block common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
- Support **actions**: `ALLOW`, `BLOCK`, `CHALLENGE` (e.g., CAPTCHA/JS), `LOG_ONLY`.
- Support **per-route policy** (e.g., stricter on `/login`, looser on `/search`) without per-request DB lookups.
- Provide **shadow mode** and explainability: for a blocked request, identify rule(s) and matched locations.
- Provide **safe rollout**: canary, staged POP rollout, instant rollback.

### Scale Targets
- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP. This is the regime where tail latency and CPU spikes define user experience.
- **Latency budget:** +2 ms p99 added by WAF at the edge. Anything higher becomes noticeable and forces teams to bypass protection.
- **Payload limits:** Up to 32 KB headers, 1 MB body (configurable per route). Hard caps prevent adversarial “slow parse” attacks.
- **Ruleset size:** 10k–50k signatures + heuristics. Large enough to be useful; small enough to keep matching predictable.

## Key Design Decisions

- **Choose: deterministic data plane with precompiled rules**
  - Rejected: dynamic rule evaluation via database calls or scripting per request
  - Why: bounded execution time is the only reliable defense under adversarial input.

- **Choose: streaming HTTP parsing + bounded body inspection**
  - Rejected: buffering entire requests for “full” inspection
  - Why: buffering creates a memory DoS vector and increases latency under chunked uploads.

- **Choose: control-plane-driven rollouts with shadow + canary**
  - Rejected: “push rules everywhere” or manual POP changes
  - Why: rules are code; unsafe rollout is a global outage waiting to happen.

## Architecture

```mermaid
flowchart LR
  C[Client] --> E[Edge Proxy]
  E --> W[WAF Filter]
  W --> O[Origin App]
  W --> L[Event Stream]
  CP[Rule Control Plane] --> RD[Rule Distributor]
  RD --> W
  L --> A[Analytics/Tuning]
```

### Components

- **Edge Proxy**
  - Terminates TLS, normalizes HTTP (canonicalization), and enforces hard limits (header size, URL length, body size, timeouts). This is where many attacks become cheap to stop.

- **WAF Filter (Data Plane)**
  - Runs inline in the proxy request path.
  - Implements: request normalization, signature matching, anomaly scoring, and action enforcement.
  - Uses only local memory and preloaded rule artifacts; no network calls.

- **Origin App**
  - Receives already-scrubbed traffic. The WAF is additive security, not a substitute for secure coding.

- **Event Stream**
  - Asynchronous security telemetry (sampled for allows, full for blocks/challenges) for tuning and incident response.

- **Rule Control Plane**
  - UI/API for rule authoring, exemptions, per-route policy, and rollout workflows.
  - Produces a signed, versioned ruleset artifact.

- **Rule Distributor**
  - Distributes artifacts to POPs, supports staged rollout, health checks, and instant rollback by version pinning.

- **Analytics/Tuning**
  - Aggregates matches, false-positive reports, and origin error correlations to drive rule improvements.

## Deep Dive: Line-Speed Detection Without Regex DoS

The core engineering problem is: **match lots of attack patterns on hostile input with a strict time budget**.

### 1) Canonicalization first, or rules are useless
Attackers win when the WAF and the origin parse differently. The data plane performs a strict normalization pass:
- Percent-decoding with limits (reject over-decode depth)
- Path normalization (`/a/../b` → `/b`) with strict RFC behavior
- Header and cookie parsing with bounded field counts and sizes
- UTF-8 validation; invalid encodings become immediate block signals

This makes matching predictable and reduces “bypass by encoding”.

### 2) Two-tier matching: cheap first, expensive rarely
All rules are compiled into bounded-time primitives:
- **Aho–Corasick** (or similar) for large sets of fixed substrings (fast multi-pattern scan).
- **RE2-style regex** only (no catastrophic backtracking). Regex is reserved for cases that genuinely need it, and is capped by input length.
- **Anomaly scoring**: individual weak signals add up; one shaky match doesn’t block by itself.

The filter runs:
1. **Cheap scans** on URL + headers + cookies (almost always enough for common attacks).
2. **Conditional body inspection** only for routes/content-types that accept user input and only up to a configured byte budget (e.g., first 64 KB of decoded fields).

### 3) Streaming body inspection with hard caps
The WAF never needs the full body to catch most real attacks, but it must not be blind:
- For `application/x-www-form-urlencoded` and `application/json`, parse incrementally and scan decoded values.
- Stop inspecting after the route’s inspection budget; continue forwarding the stream.
- If a match requires bytes beyond the budget, that’s a deliberate trade-off: the system favors availability and predictable latency.

### 4) Rollout safety is part of the detection design
Every rule is shipped with:
- **Mode** (`LOG_ONLY` → `BLOCK`)
- **Scope** (routes, methods, content-types)
- **Confidence** (used for anomaly scoring)
Rules move through a workflow: shadow → canary POPs → regional → global, with automated guardrails (block-rate deltas, origin error deltas). This is how you avoid “a ruleset update broke checkout worldwide”.

## Trade-offs

| Optimized For | Sacrificed |
|--------------|------------|
| Predictable latency under attack | Full deep-packet inspection of huge bodies |
| Safe, fast rollouts and rollback | “One-off” custom logic in the request path |
| Low operational complexity in data plane | Some detection sophistication moves to tuning/analytics |
| Consistent parsing across edge | Perfect fidelity to every origin framework quirk |

## Failure Modes

- **Bad ruleset blocks legitimate traffic**
  - What happens: sudden spikes in `BLOCK` on critical routes (login/checkout).
  - Detect: block-rate anomaly + correlated origin revenue/errors + user reports.
  - Recover: instant rollback by ruleset version pin; keep per-route emergency bypass token that downgrades to `LOG_ONLY`.

- **CPU saturation at POP during attack (tail latency blowup)**
  - What happens: proxy queues grow, p99 latency climbs, timeouts.
  - Detect: WAF CPU per request, queue depth, p99 per-route, dropped connections.
  - Recover: shed expensive work first (disable body inspection on low-risk routes), enforce stricter limits, increase challenges, then scale out.

- **Parsing differential causes bypass**
  - What happens: edge allows traffic that the origin interprets differently.
  - Detect: exploit reports, WAF/origin mismatch signatures, unusual 4xx/5xx patterns.
  - Recover: patch canonicalization, add regression tests with real framework parsers, ship fix as a high-priority ruleset + binary rollout.

## What I'd Do Differently At...

- **10x scale:**
  - Add POP-local reputation caches (hot IPs, JA3/JA4 fingerprints) and tighter adaptive challenges to cut load before deep inspection.
  - Increase automated tuning: promote rules from shadow to block based on measured false-positive rates.

- **100x scale:**
  - Split the WAF into two layers: ultra-cheap L7 gate (limits, reputation, bot/challenge) and a selective deep inspection tier for only suspicious traffic.
  - Invest in formal parser compatibility testing across common origin stacks; bypass-by-parsing becomes the dominant class of failures.

## Operational Notes

- Shadow mode is non-negotiable: run new rules as `LOG_ONLY` first and require block-rate and origin-error gates before promotion.
- Maintain strict per-route inspection budgets; the fastest WAF is the one that refuses to do unbounded work.
- Log with privacy in mind: store structured match metadata, not raw payloads, and sample allows aggressively.
- Keep an emergency “break-glass” config path that can downgrade to `ALLOW + LOG` globally when the control plane is impaired.
```

---

Review this design constructively. Find simplification opportunities and stress-test the failure modes.
mcp startup: no servers
warning: Repository snapshot encountered large untracked directories: grades (201 files). This can slow Codex; consider adding these paths to .gitignore or disabling undo in your config.




thinking
**Evaluating response structure**

I’m thinking about how to craft my response effectively. First, I want to emphasize the core insight, which is about splitting the data plane from the control plane, where the latter is responsible for rule authoring and rollout. I’ll include a table for components that deserve to be highlighted, perhaps listing around five items. Additionally, I see the need to create a table that compares simplification opportunities, looking at the current state versus alternatives and their trade-offs.
thinking
**Evaluating custom solutions and scenarios**

I'm suggesting various custom solutions, like using a proxy with Envoy or Nginx and considering eBPF for faster deployment, even if it means less control. For the custom rule distributor, pulling from S3 or xDS introduces eventual consistency, requiring a cache. Kafka or Kinesis could work for event streaming, but I'd face operational challenges. For analytics, ClickHouse with Grafana isn't fully tailored. Also, a stateless HMAC token for the challenge mechanism needs good rotation. Lastly, I should stress test five scenarios for robustness.
thinking
**Addressing system performance and security concerns**

I'm looking at ruleset issues like CPU strain from bad regex, which could be tackled by pre-deploy compile and benchmark gates, setting per-rule cost budgets, and implementing runtime circuit breakers. If traffic increases, I’d consider degrading by disabling body inspection, while incorporating admission control and rate limiting for fairness. For compromised signing keys, a solid threat model is critical, including key rotation and multi-signature checks. I need to finalize recommendations on artifact signing and handling configuration efficiently to avoid policy explosion with numerous routes and rules.
thinking
**Considering implementation strategies**

I’m exploring options like using Envoy along with WASM or Rust filters, GitOps for deployment, and object store distribution. Integrating with WAF standard rules, like the OWASP Core Rule Set, is a priority, and I want to incorporate a learning loop for user feedback to report false positives. It would be nice to have fuzz testing and differential tests as well as tools for replay and synthetic canaries. What’s working well includes clear separation, deterministic processes, and effective privacy measures. I'll keep things concise while ensuring completeness with tables, as requested.
codex
```markdown
## Elegance Check

### The Core Insight
Treat the WAF as a **deterministic, precompiled data-plane program** with strict resource caps, and push all ambiguity (authoring/tuning/rollout/analytics) into a **control plane**. This is the right axis to optimize for adversarial inputs and operational safety.

### Components That Earn Their Place
| Component | Why It's Necessary |
|-----------|-------------------|
| Edge Proxy | The cheapest place to enforce hard caps, timeouts, and canonicalization before anything expensive happens. |
| WAF Filter (Data Plane) | Guarantees bounded-time decisions with no network dependencies; the “line-speed” core. |
| Rule Control Plane | Rules are code; you need workflow, provenance, staging, and guardrails. |
| Signed, Versioned Rule Artifacts | Makes rollback, auditability, and offline POP operation possible without trusting the network. |
| Event Stream (Async Telemetry) | Enables tuning and incident response without coupling observability to request latency. |

### Simplification Opportunities
| Current | Simpler Alternative | Trade-off |
|---------|---------------------|-----------|
| Custom edge proxy + custom filter | Build as an **Envoy/Nginx module** (native filter) using existing HTTP/2/3, parsing, limits, and config plumbing | Less total control; you inherit proxy upgrade/patch cadence, but you remove a huge surface area. |
| Dedicated “Rule Distributor” service | **Pull-based distribution** from object storage (S3/GCS) fronted by CDN, with POPs polling + backoff + pinning | Slightly higher rollout latency; much simpler failure behavior (POPs keep last-known-good). |
| Bespoke rule language/runtime | Start with **OWASP CRS / ModSecurity semantics** but compile to your bounded primitives | Less bespoke expressiveness; faster maturity and clearer user mental model. |
| Custom event stream | Use **Kafka/Kinesis/PubSub** with explicit “drop-on-overload” policy at the edge | More ops cost (or managed spend); less custom reliability engineering. |
| Control-plane UI as primary authoring surface | **GitOps** (PRs + CI compile/bench + signed release) as the source of truth | Slower for emergency edits unless you add break-glass; dramatically better auditability and safety. |

## Stress Test

### Failure Scenarios
1. **Control plane or rules DB down for 5 minutes**
   - Design's answer: partially addressed (“break-glass”, version pinning implied)
   - Recommendation: **Strengthen** — make POP behavior explicit: “serve with last-known-good indefinitely,” local cache TTL policy, and a *locally configurable* global downgrade knob that does not require the control plane.

2. **Network partition between POPs and rule distribution**
   - Design's answer: not addressed (assumed by “no network calls” in data plane, but distro semantics unclear)
   - Recommendation: **Strengthen** — define pull/pin semantics, max staleness, and how you prevent “split-brain policy” during an incident (e.g., freeze upgrades unless explicit operator override).

3. **Event stream/analytics is slow or down**
   - Design's answer: implied async, but no backpressure policy described
   - Recommendation: **Must strengthen** — guarantee telemetry can never add latency: bounded in-memory queue + sampling + immediate drop once full; publish local counters even if detailed events drop.

4. **Bad ruleset causes CPU spikes (even without regex backtracking)**
   - Design's answer: “cheap first, expensive rarely,” caps mentioned
   - Recommendation: **Strengthen** — add *pre-deploy cost gates* (compile + microbench on worst-case inputs) and *runtime circuit breakers* (per-phase time budget, “disable body inspection” trigger, per-rule hit-rate anomaly auto-demotion to `LOG_ONLY`).

5. **Challenge path breaks users or becomes a new dependency**
   - Design's answer: action exists, implementation unspecified
   - Recommendation: **Strengthen** — make challenges **stateless** at the edge (HMAC-signed token/cookie with short TTL, key rotation plan) so you don’t introduce a global state store or per-request lookup.

## Recommendations

### Must Fix
- Specify **artifact trust model**: signing keys, verification in POP, rotation, and what happens on key compromise (at least “stop accepting new artifacts; pin last-known-good”).
- Define **telemetry backpressure** behavior so observability outages never become edge outages.
- Make **challenge** explicitly stateless (or explicitly justify state) and describe failure behavior (JS/CAPTCHA provider down, clock skew, token replay).
- Add **bounded execution enforcement** beyond input size (per-request CPU/time budgets and circuit breakers), not just “we intend bounded primitives.”
- Clarify **canonicalization contract** vs origin: what is normalized, what is rejected, and how you avoid breaking apps (especially Unicode, double-decode, and ambiguous path semantics).

### Should Consider
- Replace custom distributor with **object-store + pull + pin**; it matches your “data plane offline-safe” philosophy and reduces services.
- Lean on **Envoy/Nginx** for protocol edge cases (HTTP/2/3, weird clients, gRPC) to keep your “interesting” work focused on matching and rollout safety.
- Treat **content-type as attacker-controlled**: route-based inspection policy should be primary, with content-type as a hint, not a gate.
- Add a **replay + diff test harness** (captured requests + multiple origin parsers) as a first-class part of “parsing differential” defense.

### Nice to Have
- A clear **operator runbook**: “at 3am, here’s how to downgrade safely,” including guardrails to prevent permanent `ALLOW` drift.
- Explicit **privacy posture**: redaction rules, structured match metadata schema, retention defaults.
- A small set of **well-defined reason codes** for explainability that remain stable across rule versions.

## What's Working Well
- The data-plane/control-plane split is crisp and operationally sane.
- Streaming inspection with hard caps acknowledges the real DoS vectors (chunking, giant headers, slow parsing).
- Two-tier matching (Aho–Corasick + RE2-only + scoring) is a solid “bounded cost” toolkit.
- Rollout safety (shadow → canary → regional → global with rollback) is treated as part of the product, not an afterthought.
- The doc is honest about trade-offs (availability/predictability over perfect deep inspection), which is exactly what makes a WAF ownable.
```
tokens used
6,008
```markdown
## Elegance Check

### The Core Insight
Treat the WAF as a **deterministic, precompiled data-plane program** with strict resource caps, and push all ambiguity (authoring/tuning/rollout/analytics) into a **control plane**. This is the right axis to optimize for adversarial inputs and operational safety.

### Components That Earn Their Place
| Component | Why It's Necessary |
|-----------|-------------------|
| Edge Proxy | The cheapest place to enforce hard caps, timeouts, and canonicalization before anything expensive happens. |
| WAF Filter (Data Plane) | Guarantees bounded-time decisions with no network dependencies; the “line-speed” core. |
| Rule Control Plane | Rules are code; you need workflow, provenance, staging, and guardrails. |
| Signed, Versioned Rule Artifacts | Makes rollback, auditability, and offline POP operation possible without trusting the network. |
| Event Stream (Async Telemetry) | Enables tuning and incident response without coupling observability to request latency. |

### Simplification Opportunities
| Current | Simpler Alternative | Trade-off |
|---------|---------------------|-----------|
| Custom edge proxy + custom filter | Build as an **Envoy/Nginx module** (native filter) using existing HTTP/2/3, parsing, limits, and config plumbing | Less total control; you inherit proxy upgrade/patch cadence, but you remove a huge surface area. |
| Dedicated “Rule Distributor” service | **Pull-based distribution** from object storage (S3/GCS) fronted by CDN, with POPs polling + backoff + pinning | Slightly higher rollout latency; much simpler failure behavior (POPs keep last-known-good). |
| Bespoke rule language/runtime | Start with **OWASP CRS / ModSecurity semantics** but compile to your bounded primitives | Less bespoke expressiveness; faster maturity and clearer user mental model. |
| Custom event stream | Use **Kafka/Kinesis/PubSub** with explicit “drop-on-overload” policy at the edge | More ops cost (or managed spend); less custom reliability engineering. |
| Control-plane UI as primary authoring surface | **GitOps** (PRs + CI compile/bench + signed release) as the source of truth | Slower for emergency edits unless you add break-glass; dramatically better auditability and safety. |

## Stress Test

### Failure Scenarios
1. **Control plane or rules DB down for 5 minutes**
   - Design's answer: partially addressed (“break-glass”, version pinning implied)
   - Recommendation: **Strengthen** — make POP behavior explicit: “serve with last-known-good indefinitely,” local cache TTL policy, and a *locally configurable* global downgrade knob that does not require the control plane.

2. **Network partition between POPs and rule distribution**
   - Design's answer: not addressed (assumed by “no network calls” in data plane, but distro semantics unclear)
   - Recommendation: **Strengthen** — define pull/pin semantics, max staleness, and how you prevent “split-brain policy” during an incident (e.g., freeze upgrades unless explicit operator override).

3. **Event stream/analytics is slow or down**
   - Design's answer: implied async, but no backpressure policy described
   - Recommendation: **Must strengthen** — guarantee telemetry can never add latency: bounded in-memory queue + sampling + immediate drop once full; publish local counters even if detailed events drop.

4. **Bad ruleset causes CPU spikes (even without regex backtracking)**
   - Design's answer: “cheap first, expensive rarely,” caps mentioned
   - Recommendation: **Strengthen** — add *pre-deploy cost gates* (compile + microbench on worst-case inputs) and *runtime circuit breakers* (per-phase time budget, “disable body inspection” trigger, per-rule hit-rate anomaly auto-demotion to `LOG_ONLY`).

5. **Challenge path breaks users or becomes a new dependency**
   - Design's answer: action exists, implementation unspecified
   - Recommendation: **Strengthen** — make challenges **stateless** at the edge (HMAC-signed token/cookie with short TTL, key rotation plan) so you don’t introduce a global state store or per-request lookup.

## Recommendations

### Must Fix
- Specify **artifact trust model**: signing keys, verification in POP, rotation, and what happens on key compromise (at least “stop accepting new artifacts; pin last-known-good”).
- Define **telemetry backpressure** behavior so observability outages never become edge outages.
- Make **challenge** explicitly stateless (or explicitly justify state) and describe failure behavior (JS/CAPTCHA provider down, clock skew, token replay).
- Add **bounded execution enforcement** beyond input size (per-request CPU/time budgets and circuit breakers), not just “we intend bounded primitives.”
- Clarify **canonicalization contract** vs origin: what is normalized, what is rejected, and how you avoid breaking apps (especially Unicode, double-decode, and ambiguous path semantics).

### Should Consider
- Replace custom distributor with **object-store + pull + pin**; it matches your “data plane offline-safe” philosophy and reduces services.
- Lean on **Envoy/Nginx** for protocol edge cases (HTTP/2/3, weird clients, gRPC) to keep your “interesting” work focused on matching and rollout safety.
- Treat **content-type as attacker-controlled**: route-based inspection policy should be primary, with content-type as a hint, not a gate.
- Add a **replay + diff test harness** (captured requests + multiple origin parsers) as a first-class part of “parsing differential” defense.

### Nice to Have
- A clear **operator runbook**: “at 3am, here’s how to downgrade safely,” including guardrails to prevent permanent `ALLOW` drift.
- Explicit **privacy posture**: redaction rules, structured match metadata schema, retention defaults.
- A small set of **well-defined reason codes** for explainability that remain stable across rule versions.

## What's Working Well
- The data-plane/control-plane split is crisp and operationally sane.
- Streaming inspection with hard caps acknowledges the real DoS vectors (chunking, giant headers, slow parsing).
- Two-tier matching (Aho–Corasick + RE2-only + scoring) is a solid “bounded cost” toolkit.
- Rollout safety (shadow → canary → regional → global with rollback) is treated as part of the product, not an afterthought.
- The doc is honest about trade-offs (availability/predictability over perfect deep inspection), which is exactly what makes a WAF ownable.
```
[Pass 2/4] Challenge saved to: /Users/dominikdabrowski/pr/system-design-atlas/challenges/web-application-firewall.md

[Pass 3/4] Refining design...

OpenAI Codex v0.72.0 (research preview)
--------
workdir: /Users/dominikdabrowski/pr/system-design-atlas
model: gpt-5.2
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: high
reasoning summaries: auto
session id: 019b2afa-fabd-7190-8bd5-5b5f72a1bdb5
--------
user
# System Design Simplifier (v2) - The Minimalist

You are a **ruthless simplifier**. Your job is to take a system design and ask: "What's the simplest thing that could possibly work?"

## Your Philosophy

> "Perfection is achieved not when there is nothing more to add, but when there is nothing left to take away." — Antoine de Saint-Exupéry

Every component must justify its existence. If you can't articulate why removing it would break the system, remove it.

## Simplification Checklist

### 1. Component Audit
For each component, ask:
- [ ] What breaks if I remove this entirely?
- [ ] Can this be replaced with a managed service?
- [ ] Can this be merged with another component?
- [ ] Is this solving a problem we actually have?

### 2. The "Just Use X" Test
- Can we just use Postgres instead of [custom database]?
- Can we just use Redis instead of [custom cache]?
- Can we just use Kubernetes instead of [custom orchestration]?
- Can we just use S3 instead of [custom storage]?
- Can we just use Kafka instead of [custom queue]? (Or skip the queue entirely?)

### 3. Complexity Budget
A good design has:
- **1-3 custom components** (the things that make this system unique)
- **The rest is glue** (off-the-shelf, managed, boring)

If you have more than 3 custom components, you're probably over-engineering.

### 4. The "Startup Test"
Could a team of 3 engineers build and operate this? If not, simplify until they could.

## What to Simplify

### Replace Custom with Managed
- Custom metrics pipeline → Datadog/Prometheus
- Custom queue → SQS/Kafka
- Custom storage → S3/GCS
- Custom coordination → etcd/ZooKeeper
- Custom API gateway → Kong/Envoy

### Merge Components
- Separate "validator service" → validation in the main service
- Separate "config service" → config in the database
- Separate "audit service" → audit log table in the main DB

### Remove Entirely
- Caches that add complexity without proven benefit
- Queues that exist "for decoupling" but add failure modes
- Microservices that should be modules

## Output Format

Rewrite the document with:

1. **Minimal architecture** - only components that are truly necessary
2. **Clear justification** for each component that remains
3. **Explicit "What We Removed"** section explaining simplifications
4. **Honest trade-offs** - what do we give up with this simpler design?

## Rules

- **Do NOT add components** - only remove or merge
- **Do NOT add sections** - only cut or condense
- **Do NOT hedge** - if something should be removed, remove it
- **Do NOT preserve complexity** out of respect for the original

## The Test

After simplification, a reader should think:
- "Oh, that's surprisingly simple"
- "Why didn't I think of that?"
- "I could actually build this"

NOT:
- "This covers everything"
- "Very thorough"
- "Enterprise-ready"

## Output the Complete Simplified Document

Present the simplified design as THE design. No argumentative language like "we don't need X" - just present what remains as if it was always this way.

Do NOT modify the title, difficulty, or tags.

---

## Original Design

```markdown
---
title: "Web Application Firewall (WAF)"
category: "Security & Access Control"
difficulty: "Hard"
tags: [security, edge, waf, http, ddos, rules, observability]
---

## Overview

This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck. The key insight is to treat the WAF as a **deterministic, precompiled packet of logic** running in the data plane, and push all complexity—rule authoring, tuning, rollout safety, and analytics—into a separate control plane.

Most WAF attempts fail by mixing the two: they run heavy parsing and dynamic policy decisions inline, then “fix” latency with caching and exceptions until the WAF becomes a patchwork. This design keeps the data plane boring: strict resource caps, streaming parsing, bounded-time matching, and a small set of actions (allow/block/challenge/log).

## What Makes This Hard

Naive implementations get trapped in a three-way trade-off:

1. **Accuracy vs. latency:** Deep inspection and complex regex rules catch more attacks but explode CPU and tail latency under load.
2. **False positives vs. safety:** Over-blocking breaks real users; under-blocking leaves gaps. Teams end up shipping rules blindly because they lack feedback loops.
3. **Operational safety at the edge:** A bad ruleset rollout can take down *every* property globally faster than any application deploy.

The hard part is not “detect SQLi/XSS”; it’s **doing it deterministically under adversarial input** (regex bombs, giant headers, chunked bodies) while maintaining **safe, observable rule evolution**.

## Requirements

### Functional Requirements
- Detect and block common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
- Support **actions**: `ALLOW`, `BLOCK`, `CHALLENGE` (e.g., CAPTCHA/JS), `LOG_ONLY`.
- Support **per-route policy** (e.g., stricter on `/login`, looser on `/search`) without per-request DB lookups.
- Provide **shadow mode** and explainability: for a blocked request, identify rule(s) and matched locations.
- Provide **safe rollout**: canary, staged POP rollout, instant rollback.

### Scale Targets
- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP. This is the regime where tail latency and CPU spikes define user experience.
- **Latency budget:** +2 ms p99 added by WAF at the edge. Anything higher becomes noticeable and forces teams to bypass protection.
- **Payload limits:** Up to 32 KB headers, 1 MB body (configurable per route). Hard caps prevent adversarial “slow parse” attacks.
- **Ruleset size:** 10k–50k signatures + heuristics. Large enough to be useful; small enough to keep matching predictable.

## Key Design Decisions

- **Choose: deterministic data plane with precompiled rules**
  - Rejected: dynamic rule evaluation via database calls or scripting per request
  - Why: bounded execution time is the only reliable defense under adversarial input.

- **Choose: streaming HTTP parsing + bounded body inspection**
  - Rejected: buffering entire requests for “full” inspection
  - Why: buffering creates a memory DoS vector and increases latency under chunked uploads.

- **Choose: control-plane-driven rollouts with shadow + canary**
  - Rejected: “push rules everywhere” or manual POP changes
  - Why: rules are code; unsafe rollout is a global outage waiting to happen.

## Architecture

```mermaid
flowchart LR
  C[Client] --> E[Edge Proxy]
  E --> W[WAF Filter]
  W --> O[Origin App]
  W --> L[Event Stream]
  CP[Rule Control Plane] --> RD[Rule Distributor]
  RD --> W
  L --> A[Analytics/Tuning]
```

### Components

- **Edge Proxy**
  - Terminates TLS, normalizes HTTP (canonicalization), and enforces hard limits (header size, URL length, body size, timeouts). This is where many attacks become cheap to stop.

- **WAF Filter (Data Plane)**
  - Runs inline in the proxy request path.
  - Implements: request normalization, signature matching, anomaly scoring, and action enforcement.
  - Uses only local memory and preloaded rule artifacts; no network calls.

- **Origin App**
  - Receives already-scrubbed traffic. The WAF is additive security, not a substitute for secure coding.

- **Event Stream**
  - Asynchronous security telemetry (sampled for allows, full for blocks/challenges) for tuning and incident response.

- **Rule Control Plane**
  - UI/API for rule authoring, exemptions, per-route policy, and rollout workflows.
  - Produces a signed, versioned ruleset artifact.

- **Rule Distributor**
  - Distributes artifacts to POPs, supports staged rollout, health checks, and instant rollback by version pinning.

- **Analytics/Tuning**
  - Aggregates matches, false-positive reports, and origin error correlations to drive rule improvements.

## Deep Dive: Line-Speed Detection Without Regex DoS

The core engineering problem is: **match lots of attack patterns on hostile input with a strict time budget**.

### 1) Canonicalization first, or rules are useless
Attackers win when the WAF and the origin parse differently. The data plane performs a strict normalization pass:
- Percent-decoding with limits (reject over-decode depth)
- Path normalization (`/a/../b` → `/b`) with strict RFC behavior
- Header and cookie parsing with bounded field counts and sizes
- UTF-8 validation; invalid encodings become immediate block signals

This makes matching predictable and reduces “bypass by encoding”.

### 2) Two-tier matching: cheap first, expensive rarely
All rules are compiled into bounded-time primitives:
- **Aho–Corasick** (or similar) for large sets of fixed substrings (fast multi-pattern scan).
- **RE2-style regex** only (no catastrophic backtracking). Regex is reserved for cases that genuinely need it, and is capped by input length.
- **Anomaly scoring**: individual weak signals add up; one shaky match doesn’t block by itself.

The filter runs:
1. **Cheap scans** on URL + headers + cookies (almost always enough for common attacks).
2. **Conditional body inspection** only for routes/content-types that accept user input and only up to a configured byte budget (e.g., first 64 KB of decoded fields).

### 3) Streaming body inspection with hard caps
The WAF never needs the full body to catch most real attacks, but it must not be blind:
- For `application/x-www-form-urlencoded` and `application/json`, parse incrementally and scan decoded values.
- Stop inspecting after the route’s inspection budget; continue forwarding the stream.
- If a match requires bytes beyond the budget, that’s a deliberate trade-off: the system favors availability and predictable latency.

### 4) Rollout safety is part of the detection design
Every rule is shipped with:
- **Mode** (`LOG_ONLY` → `BLOCK`)
- **Scope** (routes, methods, content-types)
- **Confidence** (used for anomaly scoring)
Rules move through a workflow: shadow → canary POPs → regional → global, with automated guardrails (block-rate deltas, origin error deltas). This is how you avoid “a ruleset update broke checkout worldwide”.

## Trade-offs

| Optimized For | Sacrificed |
|--------------|------------|
| Predictable latency under attack | Full deep-packet inspection of huge bodies |
| Safe, fast rollouts and rollback | “One-off” custom logic in the request path |
| Low operational complexity in data plane | Some detection sophistication moves to tuning/analytics |
| Consistent parsing across edge | Perfect fidelity to every origin framework quirk |

## Failure Modes

- **Bad ruleset blocks legitimate traffic**
  - What happens: sudden spikes in `BLOCK` on critical routes (login/checkout).
  - Detect: block-rate anomaly + correlated origin revenue/errors + user reports.
  - Recover: instant rollback by ruleset version pin; keep per-route emergency bypass token that downgrades to `LOG_ONLY`.

- **CPU saturation at POP during attack (tail latency blowup)**
  - What happens: proxy queues grow, p99 latency climbs, timeouts.
  - Detect: WAF CPU per request, queue depth, p99 per-route, dropped connections.
  - Recover: shed expensive work first (disable body inspection on low-risk routes), enforce stricter limits, increase challenges, then scale out.

- **Parsing differential causes bypass**
  - What happens: edge allows traffic that the origin interprets differently.
  - Detect: exploit reports, WAF/origin mismatch signatures, unusual 4xx/5xx patterns.
  - Recover: patch canonicalization, add regression tests with real framework parsers, ship fix as a high-priority ruleset + binary rollout.

## What I'd Do Differently At...

- **10x scale:**
  - Add POP-local reputation caches (hot IPs, JA3/JA4 fingerprints) and tighter adaptive challenges to cut load before deep inspection.
  - Increase automated tuning: promote rules from shadow to block based on measured false-positive rates.

- **100x scale:**
  - Split the WAF into two layers: ultra-cheap L7 gate (limits, reputation, bot/challenge) and a selective deep inspection tier for only suspicious traffic.
  - Invest in formal parser compatibility testing across common origin stacks; bypass-by-parsing becomes the dominant class of failures.

## Operational Notes

- Shadow mode is non-negotiable: run new rules as `LOG_ONLY` first and require block-rate and origin-error gates before promotion.
- Maintain strict per-route inspection budgets; the fastest WAF is the one that refuses to do unbounded work.
- Log with privacy in mind: store structured match metadata, not raw payloads, and sample allows aggressively.
- Keep an emergency “break-glass” config path that can downgrade to `ALLOW + LOG` globally when the control plane is impaired.
```

---

## Critique to Address

```markdown
## Elegance Check

### The Core Insight
Treat the WAF as a **deterministic, precompiled data-plane program** with strict resource caps, and push all ambiguity (authoring/tuning/rollout/analytics) into a **control plane**. This is the right axis to optimize for adversarial inputs and operational safety.

### Components That Earn Their Place
| Component | Why It's Necessary |
|-----------|-------------------|
| Edge Proxy | The cheapest place to enforce hard caps, timeouts, and canonicalization before anything expensive happens. |
| WAF Filter (Data Plane) | Guarantees bounded-time decisions with no network dependencies; the “line-speed” core. |
| Rule Control Plane | Rules are code; you need workflow, provenance, staging, and guardrails. |
| Signed, Versioned Rule Artifacts | Makes rollback, auditability, and offline POP operation possible without trusting the network. |
| Event Stream (Async Telemetry) | Enables tuning and incident response without coupling observability to request latency. |

### Simplification Opportunities
| Current | Simpler Alternative | Trade-off |
|---------|---------------------|-----------|
| Custom edge proxy + custom filter | Build as an **Envoy/Nginx module** (native filter) using existing HTTP/2/3, parsing, limits, and config plumbing | Less total control; you inherit proxy upgrade/patch cadence, but you remove a huge surface area. |
| Dedicated “Rule Distributor” service | **Pull-based distribution** from object storage (S3/GCS) fronted by CDN, with POPs polling + backoff + pinning | Slightly higher rollout latency; much simpler failure behavior (POPs keep last-known-good). |
| Bespoke rule language/runtime | Start with **OWASP CRS / ModSecurity semantics** but compile to your bounded primitives | Less bespoke expressiveness; faster maturity and clearer user mental model. |
| Custom event stream | Use **Kafka/Kinesis/PubSub** with explicit “drop-on-overload” policy at the edge | More ops cost (or managed spend); less custom reliability engineering. |
| Control-plane UI as primary authoring surface | **GitOps** (PRs + CI compile/bench + signed release) as the source of truth | Slower for emergency edits unless you add break-glass; dramatically better auditability and safety. |

## Stress Test

### Failure Scenarios
1. **Control plane or rules DB down for 5 minutes**
   - Design's answer: partially addressed (“break-glass”, version pinning implied)
   - Recommendation: **Strengthen** — make POP behavior explicit: “serve with last-known-good indefinitely,” local cache TTL policy, and a *locally configurable* global downgrade knob that does not require the control plane.

2. **Network partition between POPs and rule distribution**
   - Design's answer: not addressed (assumed by “no network calls” in data plane, but distro semantics unclear)
   - Recommendation: **Strengthen** — define pull/pin semantics, max staleness, and how you prevent “split-brain policy” during an incident (e.g., freeze upgrades unless explicit operator override).

3. **Event stream/analytics is slow or down**
   - Design's answer: implied async, but no backpressure policy described
   - Recommendation: **Must strengthen** — guarantee telemetry can never add latency: bounded in-memory queue + sampling + immediate drop once full; publish local counters even if detailed events drop.

4. **Bad ruleset causes CPU spikes (even without regex backtracking)**
   - Design's answer: “cheap first, expensive rarely,” caps mentioned
   - Recommendation: **Strengthen** — add *pre-deploy cost gates* (compile + microbench on worst-case inputs) and *runtime circuit breakers* (per-phase time budget, “disable body inspection” trigger, per-rule hit-rate anomaly auto-demotion to `LOG_ONLY`).

5. **Challenge path breaks users or becomes a new dependency**
   - Design's answer: action exists, implementation unspecified
   - Recommendation: **Strengthen** — make challenges **stateless** at the edge (HMAC-signed token/cookie with short TTL, key rotation plan) so you don’t introduce a global state store or per-request lookup.

## Recommendations

### Must Fix
- Specify **artifact trust model**: signing keys, verification in POP, rotation, and what happens on key compromise (at least “stop accepting new artifacts; pin last-known-good”).
- Define **telemetry backpressure** behavior so observability outages never become edge outages.
- Make **challenge** explicitly stateless (or explicitly justify state) and describe failure behavior (JS/CAPTCHA provider down, clock skew, token replay).
- Add **bounded execution enforcement** beyond input size (per-request CPU/time budgets and circuit breakers), not just “we intend bounded primitives.”
- Clarify **canonicalization contract** vs origin: what is normalized, what is rejected, and how you avoid breaking apps (especially Unicode, double-decode, and ambiguous path semantics).

### Should Consider
- Replace custom distributor with **object-store + pull + pin**; it matches your “data plane offline-safe” philosophy and reduces services.
- Lean on **Envoy/Nginx** for protocol edge cases (HTTP/2/3, weird clients, gRPC) to keep your “interesting” work focused on matching and rollout safety.
- Treat **content-type as attacker-controlled**: route-based inspection policy should be primary, with content-type as a hint, not a gate.
- Add a **replay + diff test harness** (captured requests + multiple origin parsers) as a first-class part of “parsing differential” defense.

### Nice to Have
- A clear **operator runbook**: “at 3am, here’s how to downgrade safely,” including guardrails to prevent permanent `ALLOW` drift.
- Explicit **privacy posture**: redaction rules, structured match metadata schema, retention defaults.
- A small set of **well-defined reason codes** for explainability that remain stable across rule versions.

## What's Working Well
- The data-plane/control-plane split is crisp and operationally sane.
- Streaming inspection with hard caps acknowledges the real DoS vectors (chunking, giant headers, slow parsing).
- Two-tier matching (Aho–Corasick + RE2-only + scoring) is a solid “bounded cost” toolkit.
- Rollout safety (shadow → canary → regional → global with rollback) is treated as part of the product, not an afterthought.
- The doc is honest about trade-offs (availability/predictability over perfect deep inspection), which is exactly what makes a WAF ownable.
```

---

## Your Task

Produce the **final, elegant design** that:
1. Incorporates valid simplifications from the critique
2. Addresses failure scenarios raised
3. Removes unnecessary complexity
4. Remains practical and buildable by a small team

The result should make readers think "that's surprisingly simple" not "very thorough."

Output the complete, final markdown document.
mcp startup: no servers
warning: Repository snapshot encountered large untracked directories: grades (201 files). This can slow Codex; consider adding these paths to .gitignore or disabling undo in your config.





thinking
**Simplifying architecture components**

I'm considering how to simplify the architecture by removing the Analytics and Tuning components, but I'll still need telemetry for tuning. Perhaps I could log this data to object storage, although that's new for me. We can maintain the Event Stream as a managed log ingestion with minimal requirements. I need to ensure a clear justification for each component, so I should adjust the architecture diagram accordingly. We'll also integrate analytics and tuning into the control plane for better simplicity.
thinking
**Thinking about artifact hosting and documentation**

I need to clarify that artifact hosting could be part of the rule control plane, even if that means adding a component. I can mention that the Rule Control Plane produces signed artifacts at a stable URL which the POPs can poll. For simplifying the rule language, I’ll start with OWASP CRS semantics but can't add components. I plan to rewrite the document while keeping the frontmatter the same and adjusting sections, removing unnecessary parts, and condensing where possible.
thinking
**Detailing implementation and security models**

I'm working on the implementation details for the artifact trust model by focusing on offline signature verification and ensuring the POP pins the last-known-good state. Key rotation is crucial; if a key is compromised, I should stop accepting new signatures and distribute a new key via a binary update. The telemetry system can utilize a local bounded queue for sampling and counters, while the runtime circuit breakers need careful thought on security impacts and safe degradation options. I'll also address pre-deploy cost gates in the control plane pipeline for better efficiency.
thinking
**Addressing ruleset CPU spikes**

I'm considering how to manage CPU spikes from the ruleset. Options include implementing canary testing, cost gates, runtime circuit breakers, and maybe an auto-demote feature for per-rule anomalies. However, I think a simpler approach is best: if the WAF time budget exceeds limits or CPU usage is high, I would disable body inspection and switch to CHALLENGE for high-risk routes. For the canonicalization contract, I'll ensure strict normalization and choose a single canonicalization profile per route. I need to finalize the markdown document clearly without hedging. I’ll also rename the scaling section to "What We Removed" rather than adding new sections.
thinking
**Planning markdown document updates**

I see that the output format needs to reflect “minimal architecture” within the architecture section, which is possible without adding new headings. I can also include subheadings under existing components. The document includes a Mermaid diagram, so I'll update that to reflect the simplifications. Although the user asked for a markdown document, there's ambiguity since they might want to modify an existing file in a repository. To be proactive, I'll locate the WAF markdown file, update it, and ensure everything meets the required standards. My process will include finding the document, rewriting it, applying changes, and checking the formatting.
Plan update
  → Locate existing WAF markdown file
  • Draft simplified design content
  • Update file and diagrams
  • Sanity-check markdown rendering
exec
/bin/zsh -lc "rg -n \"AGENTS\\.md\" -S ." in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 73ms:
./logs/calendar-scheduling-system.log:1955:/bin/zsh -lc 'find .. -name AGENTS.md -print' in /Users/dominikdabrowski/pr/system-design-atlas exited 124 in 10.01s:
./logs/calendar-scheduling-system.log:1961:/bin/zsh -lc 'find . -name AGENTS.md -print' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 580ms:
./logs/live-audio-rooms.log:4631:/bin/zsh -lc 'find .. -maxdepth 2 -name AGENTS.md -print' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 93ms:
./logs/live-audio-rooms.log:4632:../semper/AGENTS.md
./logs/live-audio-rooms.log:4633:../24h_modal/AGENTS.md
./logs/live-audio-rooms.log:4634:../betterdocs/AGENTS.md
./logs/live-audio-rooms.log:4635:../kafkette/AGENTS.md
./logs/cross-region-shopping-cart.log:1430:/bin/zsh -lc 'find .. -name AGENTS.md -print' in /Users/dominikdabrowski/pr/system-design-atlas exited 124 in 10.01s:
./logs/cross-region-shopping-cart.log:1433:/bin/zsh -lc 'find . -name AGENTS.md -print' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 541ms:
./logs/v2/real-time-ad-serving.log:168:/bin/zsh -lc 'find .. -name AGENTS.md -print' in /Users/dominikdabrowski/pr/system-design-atlas exited 124 in 10.00s:
./logs/v2/real-time-ad-serving.log:171:/bin/zsh -lc 'find . -maxdepth 4 -name AGENTS.md -print' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 408ms:
./logs/v2/high-frequency-trading-gateway.log:1193:I think it would be helpful to implement changes, even though the user didn't specifically ask. Given that typical tasks involve editing repository docs in the Codex CLI environment, I'll go ahead and search for the design file using `rg`. I'll make sure to follow any AGENTS instructions, but I need to check if the root AGENTS are included since the developer message isn’t clear on that. I’ll run `ls` to locate AGENTS.md if it exists. Since our approval policy is set to never, I can run commands freely within the sandbox. The task seems moderate: find the file and update it, so I’ll use the plan tool for the multiple steps involved. Let’s get started!
exec
/bin/zsh -lc "rg -n \"title: \\\"Web Application Firewall \\(WAF\\)\\\"\" -S ." in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 58ms:
./reviewed/web-application-firewall.md:2:title: "Web Application Firewall (WAF)"
./drafts-v2/web-application-firewall.md:3:title: "Web Application Firewall (WAF)"
./grades/web-application-firewall.log:52:title: "Web Application Firewall (WAF)"
./logs/v2/web-application-firewall.log:166:title: "Web Application Firewall (WAF)"
./logs/v2/web-application-firewall.log:339:title: "Web Application Firewall (WAF)"
./logs/v2/web-application-firewall.log:633:title: "Web Application Firewall (WAF)"
./logs/v2/web-application-firewall.log:1090:title: "Web Application Firewall (WAF)"
./problems.yaml:486:    title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:245:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:553:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:942:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:1254:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:1738:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:2313:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:2832:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:3042:title: "Web Application Firewall (WAF)"
./drafts/web-application-firewall.md:2:title: "Web Application Firewall (WAF)"
./solutions/08-security-access-control/web-application-firewall.md:2:title: "Web Application Firewall (WAF)"
exec
/bin/zsh -lc ls in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 49ms:
challenges
drafts
drafts-v2
frontend
grades
logs
problems.yaml
prompts
reviewed
scripts
solutions
solutions-v2
exec
/bin/zsh -lc "rg -n \"Web Application Firewall \\(WAF\\)\" -S ." in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 102ms:
./problems.yaml:486:    title: "Web Application Firewall (WAF)"
./drafts-v2/web-application-firewall.md:3:title: "Web Application Firewall (WAF)"
./drafts/web-application-firewall.md:2:title: "Web Application Firewall (WAF)"
./drafts/web-application-firewall.md:10:A Web Application Firewall (WAF) is an edge security layer that inspects HTTP(S) traffic to block common application attacks (SQL injection, XSS, malicious payloads, protocol abuses) before they reach origin services. The challenge is achieving *high detection quality* while preserving *line-rate performance* and *low latency*, under adversarial traffic patterns designed to maximize CPU, memory, and state pressure.
./logs/web-application-firewall.log:2:Generating: Web Application Firewall (WAF)
./logs/web-application-firewall.log:224:**Title**: Web Application Firewall (WAF)
./logs/web-application-firewall.log:237:The user wants a comprehensive markdown document for a Web Application Firewall (WAF), including details like requirements, architecture, and components such as Edge POP and Rule Engine. I'll incorporate diagrams and ensure the Mermaid syntax is valid and straightforward. Key sections should cover the data model, APIs for managing rules, scaling factors like kernel bypass, and monitoring strategies. I need to provide clear trade-offs and failure modes while structuring the document with YAML frontmatter, detailed enough to convey complexity while remaining concise.
./logs/web-application-firewall.log:245:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:253:A Web Application Firewall (WAF) is an edge security layer that inspects HTTP(S) traffic to block common application attacks (SQL injection, XSS, malicious payloads, protocol abuses) before they reach origin services. The challenge is achieving *high detection quality* while preserving *line-rate performance* and *low latency*, under adversarial traffic patterns designed to maximize CPU, memory, and state pressure.
./logs/web-application-firewall.log:553:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:561:A Web Application Firewall (WAF) is an edge security layer that inspects HTTP(S) traffic to block common application attacks (SQL injection, XSS, malicious payloads, protocol abuses) before they reach origin services. The challenge is achieving *high detection quality* while preserving *line-rate performance* and *low latency*, under adversarial traffic patterns designed to maximize CPU, memory, and state pressure.
./logs/web-application-firewall.log:942:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:950:A Web Application Firewall (WAF) is an edge security layer that inspects HTTP(S) traffic to block common application attacks (SQL injection, XSS, malicious payloads, protocol abuses) before they reach origin services. The challenge is achieving *high detection quality* while preserving *line-rate performance* and *low latency*, under adversarial traffic patterns designed to maximize CPU, memory, and state pressure.
./logs/web-application-firewall.log:1254:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:1262:A Web Application Firewall (WAF) is an edge security layer that inspects HTTP(S) traffic to detect and mitigate application-layer attacks (e.g., SQL injection, XSS, path traversal, SSRF, protocol evasion, malformed payloads) before requests reach origin services.
./logs/web-application-firewall.log:1738:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:1746:A Web Application Firewall (WAF) is an edge security layer that inspects HTTP(S) traffic to detect and mitigate application-layer attacks (e.g., SQL injection, XSS, path traversal, SSRF, protocol evasion, malformed payloads) before requests reach origin services.
./logs/web-application-firewall.log:2313:title: "Web Application Firewall (WAF)"
./logs/web-application-firewall.log:2321:A Web Application Firewall (WAF) is an edge security layer that inspects HTTP(S) traffic to detect and mitigate application-layer attacks (e.g., SQL injection, XSS, path traversal, SSRF, protocol evasion, malformed payloads) before requests reach origin services.
./logs/web-application-firewall.log:2832:title: "Web Application Firewall (WAF)"

thinking
**Searching for AGENTS.md**

I initially searched for AGENTS using rg, but that just scanned the logs instead of the actual file. Now I need to locate the real AGENTS.md in the repo. I can use `find . -name AGENTS.md -print` or `rg --files AGENTS.md` to find it. It’s time to get started on that!
exec
/bin/zsh -lc 'find . -name AGENTS.md -print' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 761ms:


thinking
**Choosing files to update**

Alright, there's no AGENTS in the repo, which is good! Now, I need to decide which file to update. The user mentioned "Original Design," but it doesn't exactly match any existing file. I should look into current versions of either `drafts-v2/web-application-firewall.md` or `reviewed/web-application-firewall.md`. It seems like `drafts-v2/web-application-firewall.md` exists, so I'll open it and compare it with the original. I think I'll use `sed -n` to check both drafts and reviewed files.
exec
/bin/zsh -lc "sed -n '1,220p' solutions/08-security-access-control/web-application-firewall.md" in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 45ms:
---
title: "Web Application Firewall (WAF)"
category: "Security & Access Control"
difficulty: "Hard"
tags: ["waf", "edge-security", "appsec", "ddos-mitigation", "rate-limiting", "observability"]
---

## Overview

A Web Application Firewall (WAF) is an edge security layer that inspects inbound HTTP(S) traffic and mitigates common application-layer attacks (SQLi, XSS, SSRF, path traversal, protocol evasion, malformed requests) before requests reach origin services.

This design uses:
- A **fast, deterministic edge data plane** that evaluates requests under strict CPU/bytes limits.
- A **central control plane** that authors, validates, versions, and safely rolls out policies as signed, immutable bundles.

---

## Requirements

### Goals
exec
/bin/zsh -lc "sed -n '1,220p' reviewed/web-application-firewall.md" in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 43ms:
---
title: "Web Application Firewall (WAF)"
category: "Security & Access Control"
difficulty: "Hard"
tags: ["waf", "edge-security", "appsec", "ddos-mitigation", "rate-limiting", "observability"]
---

## Overview

A Web Application Firewall (WAF) is an edge security layer that inspects HTTP(S) traffic to detect and mitigate application-layer attacks (e.g., SQL injection, XSS, path traversal, SSRF, protocol evasion, malformed payloads) before requests reach origin services.

The core systems challenge is adversarial: attackers actively craft traffic to maximize false negatives (bypass) and false positives (denial), while also attempting to exhaust CPU, memory, connection state, and logging pipelines. A production WAF must deliver **high detection quality** under **tight latency and compute budgets**, at **global scale**, with **safe configuration and rollouts**.

A robust design separates:
- **Data plane (edge)**: a fast, deterministic, mostly stateless request evaluation pipeline with strict resource bounds and predictable worst-case behavior.
- **Control plane (central)**: strongly-consistent policy authoring, validation, staged rollout, audit, and rollback, with eventual propagation to edge PoPs.

---

## Requirements
exec
/bin/zsh -lc "sed -n '1,220p' drafts-v2/web-application-firewall.md" in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 43ms:
```markdown
---
title: "Web Application Firewall (WAF)"
category: "Security & Access Control"
difficulty: "Hard"
tags: [security, edge, waf, http, ddos, rules, observability]
---

## Overview

This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck. The key insight is to treat the WAF as a **deterministic, precompiled packet of logic** running in the data plane, and push all complexity—rule authoring, tuning, rollout safety, and analytics—into a separate control plane.

Most WAF attempts fail by mixing the two: they run heavy parsing and dynamic policy decisions inline, then “fix” latency with caching and exceptions until the WAF becomes a patchwork. This design keeps the data plane boring: strict resource caps, streaming parsing, bounded-time matching, and a small set of actions (allow/block/challenge/log).

## What Makes This Hard

Naive implementations get trapped in a three-way trade-off:

1. **Accuracy vs. latency:** Deep inspection and complex regex rules catch more attacks but explode CPU and tail latency under load.
2. **False positives vs. safety:** Over-blocking breaks real users; under-blocking leaves gaps. Teams end up shipping rules blindly because they lack feedback loops.


thinking
**Evaluating the design output**

I see that the prompt includes an original design within a markdown code block. It suggests that I should create a simplified design for a document titled `reviewed/web-application-firewall.md`. However, since the prompt indicates it should be a final markdown document, I should ensure it reflects the final reviewed structure.

I'll check the current content of `reviewed/web-application-firewall.md`, which seems to contain sections not found in the original prompt. I might need to produce a simplified document and update it accordingly.
thinking
**Clarifying markdown document update**

The instructions state to "Output the complete, final markdown document," which suggests I should update `reviewed/web-application-firewall.md` to reflect the simplified version. While I need to preserve the original title and tags, that means I’ll have to match the frontmatter from the original, not from the reviewed file.

It seems like I should work with `drafts-v2/web-application-firewall.md` for the simplified document, replacing the original while keeping the correct frontmatter. I probably need to check other v2 draft files for consistency in the pattern used.
exec
/bin/zsh -lc 'ls drafts-v2 | head' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 141ms:
alerting-incident-routing.md
api-gateway.md
attribution-measurement-system.md
audit-trail-system.md
authorization-service-zanzibar.md
calendar-scheduling-system.md
capacity-planning-autoscaling.md
centralized-logging-platform.md
certificate-authority-pki.md
change-data-capture-platform.md
exec
/bin/zsh -lc "sed -n '1,40p' drafts-v2/api-gateway.md" in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 44ms:
```markdown
---
title: "API Gateway"
category: "IoT & Edge"
difficulty: "Medium"
tags: ["api-gateway", "edge", "envoy", "rate-limiting", "circuit-breaking", "grpc", "transcoding"]
---

## Overview

This system is a unified entry point for IoT and edge traffic that enforces policy (routing, quotas, and safety) while translating client-friendly JSON/HTTP into internal gRPC. The key insight is to treat the gateway as a **fast, dumb data plane** (packet-level decisions, no business logic) driven by a **small control plane** (policy + config distribution). That split keeps latency low at the edge and keeps policy changes safe and auditable.

Elegance comes from solving the one hard problem—**enforcing multi-tenant limits and resilience without turning the gateway into a centralized bottleneck**—with a two-level approach: local, deterministic enforcement on each gateway instance plus a lightweight global limiter for fairness across the fleet. Everything else stays boring: Envoy for the data plane, protobuf as the source of truth, and standard observability primitives.

## What Makes This Hard

Naive gateways centralize every decision (rate limit checks, auth introspection, routing lookups). At IoT scale, that becomes self-inflicted DDoS: the gateway collapses precisely when traffic spikes or backends degrade. The trap is “strong global consistency for limits” — it looks correct on paper and fails in production.

The second trap is protocol translation without a schema contract. “Just map JSON to gRPC” turns into silent breaking changes, inconsistent error models, and impossible debugging across device firmware versions. Translation must be anchored on a single canonical contract and versioning strategy.

exec
/bin/zsh -lc 'ls solutions-v2 | head' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 46ms:
01-foundational-infrastructure
02-storage-data-platforms
03-observability-reliability
04-realtime-media
05-social-discovery
06-commerce-fintech
07-ai-ml-infrastructure
08-security-access-control
09-iot-edge
10-strategic-problems
exec
/bin/zsh -lc "sed -n '1,30p' solutions-v2/09-iot-edge/api-gateway.md" in /Users/dominikdabrowski/pr/system-design-atlas exited 1 in 50ms:
sed: solutions-v2/09-iot-edge/api-gateway.md: No such file or directory
exec
/bin/zsh -lc 'ls solutions-v2/09-iot-edge | head' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 95ms:
calendar-scheduling-system.md
developer-portal.md
smart-home-hub.md

thinking
**Exploring solution drafts**

I’m realizing that solutions-v2 might not contain all drafts, maybe just a subset. I wonder if WAF is in the solutions-v2/08-security-access-control? It seems logical to check there first. It's always interesting trying to piece together where everything might be stored, especially when I'm trying to ensure I have access to the complete information! Let's dig into that.
exec
/bin/zsh -lc 'ls solutions-v2/08-security-access-control | rg -n "waf|firewall" || true' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 49ms:



thinking
**Evaluating WAF solutions**

I see there's no Web Application Firewall (WAF) included in the solutions for v2, and I'm wondering if it's just not incorporated yet. There's a task about simplifying a document. I think the expectation might be to simplify the `reviewed/web-application-firewall.md` file, merging it with the draft version while keeping the original tags intact. I’m weighing whether to update the reviewed document or to prioritize maintaining the original. It's a bit tricky, but I’m trying to figure this out!
thinking
**Considering document updates**

I’m thinking that the tasks might run the simplifier to create a "reviewed" version. There’s an instruction to output a complete, final markdown document, but it doesn't specifically say to update a file. Typically, tasks modify the `reviewed` or `solutions-v2`, so I'm leaning toward updating `reviewed/web-application-firewall.md` to reflect a simplified design. However, since this file seems previously generated and not minimal, the real goal could be to create a minimal design document for v2. I’ll need to check `problems.yaml` for insights!
exec
/bin/zsh -lc "python - <<'PY'
import yaml
from pathlib import Path
p=Path('problems.yaml')
obj=yaml.safe_load(p.read_text())
# find entries with title
matches=[]
for i,item in enumerate(obj):
    if isinstance(item,dict) and item.get('title')=='Web Application Firewall (WAF)':
        matches.append((i,item))
print('matches',len(matches))
for i,item in matches:
    print('index',i)
    for k in ['title','path','difficulty','category','tags','version']:
        if k in item: print(k, item[k])
PY" in /Users/dominikdabrowski/pr/system-design-atlas exited 127 in 47ms:
zsh:1: command not found: python
exec
/bin/zsh -lc "python3 - <<'PY'
import yaml
from pathlib import Path
obj=yaml.safe_load(Path('problems.yaml').read_text())
for i,item in enumerate(obj):
    if isinstance(item,dict) and item.get('title')=='Web Application Firewall (WAF)':
        print('index',i)
        for k in ['title','path','difficulty','category','tags','version']:
            if k in item:
                print(f'{k}: {item[k]}')
PY" in /Users/dominikdabrowski/pr/system-design-atlas exited 1 in 123ms:
Traceback (most recent call last):
  File "<stdin>", line 1, in <module>
ModuleNotFoundError: No module named 'yaml'

thinking
**Exploring YAML alternatives**

It seems PyYAML isn’t installed, which is a bit of a hiccup! I might consider using `ruby` as an alternative. It could be more efficient to manipulate the file using `sed` around line 486 in problems.yaml. To see line numbers clearly, I can use `nl -ba` along with `sed`. Let's see how this works out; hopefully, it’ll solve the problem and make things easier!
exec
/bin/zsh -lc "nl -ba problems.yaml | sed -n '450,520p'" in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 51ms:
   450	    title: "Authorization Service (Zanzibar-style)"
   451	    category: "Security & Access Control"
   452	    category_dir: "08-security-access-control"
   453	    description: "Design a global relationship-based access control (ReBAC) system for resolving permissions at low latency."
   454	
   455	  - slug: "identity-provider"
   456	    title: "Identity Provider (OAuth2/OIDC)"
   457	    category: "Security & Access Control"
   458	    category_dir: "08-security-access-control"
   459	    description: "Design a centralized auth service supporting SSO, MFA, device trust scoring, and session revocation."
   460	
   461	  - slug: "right-to-be-forgotten-pipeline"
   462	    title: "Right to be Forgotten Pipeline"
   463	    category: "Security & Access Control"
   464	    category_dir: "08-security-access-control"
   465	    description: "Design a deletion framework that purges user data across primary DBs, caches, cold storage, and analytical warehouses."
   466	
   467	  - slug: "bot-detection-system"
   468	    title: "Bot Detection System"
   469	    category: "Security & Access Control"
exec
/bin/zsh -lc 'ls solutions-v2/08-security-access-control | head' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 45ms:
authorization-service-zanzibar.md
identity-provider.md
key-management-system.md
zero-trust-access-proxy.md

thinking
**Checking document standards**

I’m not entirely sure about the standards for v2. It seems that “drafts-v2” might hold the initial documents while “reviewed” contains the finalized ones. I wonder if there’s another example of something in drafts-v2 that has a counterpart in reviewed. For instance, I think "api-gateway" is in drafts-v2, so I might want to look for `reviewed/api-gateway.md` to confirm if it exists.
exec
/bin/zsh -lc 'test -f reviewed/api-gateway.md && echo yes || echo no' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 43ms:
yes
exec
/bin/zsh -lc "sed -n '1,40p' reviewed/api-gateway.md" in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 47ms:
---
title: "API Gateway"
category: "IoT & Edge"
difficulty: "Hard"
tags: ["api-gateway", "edge", "rate-limiting", "envoy", "xds", "ddos", "waf"]
---

## Overview

An **API Gateway** is the unified entry point for external clients—IoT devices, mobile/web apps, and partners—into a fleet of backend services. It terminates connections, authenticates and authorizes requests, applies traffic policy (rate limits/quotas), performs routing and protocol translation (e.g., gRPC ↔ JSON/HTTP), and protects upstreams with resilience controls (timeouts, retries, circuit breaking).

The core challenge is that the gateway sits on the **fault line** between the public internet and internal systems:

- Traffic is **bursty** and often adversarial (DDoS, credential stuffing, abusive tenants).
- Identities are **high-cardinality** (millions of device certs/API keys).
- Networks are **heterogeneous** (cellular, intermittent connectivity, NATs, proxies).
- Configuration changes are frequent and potentially dangerous (“bad config” outages).

A production-ready design separates the system into:
- **Data plane**: high-performance, horizontally scaled proxies that handle requests.


thinking
**Reviewing document structure**

I see that the review is more thorough and has a hard difficulty level, unlike the medium difficulty of drafts-v2 for the api-gateway. It seems like there's a separate or possibly older dataset for the review. I'm considering updating `drafts-v2/web-application-firewall.md`, but it's currently fenced. Maybe the v2 generator keeps drafts with fences for raw markdown in the prompt? Ultimately, I think I’ll simplify the content in `drafts-v2` while adhering to the original format elements.
thinking
**Exploring file structure**

I'm considering how each file begins and ends with Markdown fences, which suggests they might serve as prompts for “Simplifier” and "Solution" tasks. The focus is on "System Design Simplifier (v2)," aimed at taking complex designs and simplifying them. Perhaps the outputs will be found in `solutions-v2` or `drafts-v2`. It seems `drafts-v2` could house results from the "Hard v2" generator, leading to simplified outcomes in `solutions-v2`. I should check out the first lines of `solutions-v2/08-security-access-control/authorization-service-zanzibar.md` for clarity.
exec
/bin/zsh -lc "sed -n '1,40p' solutions-v2/08-security-access-control/authorization-service-zanzibar.md" in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 46ms:
```markdown
---
title: "Authorization Service (Zanzibar-style)"
category: "Security & Access Control"
difficulty: "Hard"
tags: ["authorization", "rebac", "zanzibar", "global", "low-latency", "consistency"]
---

## Overview

This is a global relationship-based access control (ReBAC) service in the Zanzibar family: you write relationship tuples (e.g., `doc:123#viewer@user:7`) and query whether a principal has a permission on an object.

The system is built around one contract: **writes return a revision token; reads either use a stable snapshot by default or request a minimum revision**. Every response includes `served_revision`. This makes correctness debuggable, makes read-your-writes explicit, and keeps performance optimizations from leaking into product semantics.

## What Makes This Hard

ReBAC checks can explode into many edges through groups/roles/nesting. The hard part isn’t “fast traversal”; it’s **bounded evaluation** (limits, cycle handling, time budgets) and **consistent snapshots** so answers don’t flap after writes.

## Requirements




thinking
**Verifying document details**

I'm noticing that the Solutions-v2 file contains fenced content, which is typical for v2. There's a reference to the authorization service, Zanzibar, also in fenced content. It seems like I should update the WAF document in solutions-v2, but the file doesn't exist. I wonder if there’s a corresponding slug for each solution. I see a few problems in the YAML file, which tells me there's some missing content. Since the user asked to simplify design, it still might be useful to explore creating a WAF solution. I'll check further.
thinking
**Deciding on document modifications**

I need to follow instructions regarding the WAF documentation: “Do not modify the title, difficulty, or tags.” So, it seems I should keep the original format of the tags, which are unquoted in drafts-v2. However, in solutions-v2, they’re quoted. The user prefers no modifications to the tags, so I guess I’ll leave them as they are.

I'm also reminded that I can't add components but can only remove or merge, which helps clarify my task. First, I will locate the WAF file, which I’ve done, and now I need to determine where to add it in solutions-v2.
thinking
**Evaluating document rewrite options**

The prompt suggests rewriting the document, which means I might consider updating the design file rather than creating a new one. However, the user specifically asked for a rewrite of the document provided. It seems likely that this document resides in `drafts-v2`, which directly matches the original. 

Updating it may overwrite the original design, but that could be acceptable. Since the repo focuses on generating solutions, I should aim to create a simplified version instead. I need to explore if "System Design Simplifier" means making a new file in `solutions-v2`, so I should check the `prompts` directory next.
exec
/bin/zsh -lc 'ls prompts | head' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 57ms:
agent-frontend.md
system-generate.md
system-review.md
system-simplify.md
v2
exec
/bin/zsh -lc 'ls prompts/v2' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 84ms:
system-challenge.md
system-generate.md
system-review.md
system-simplify.md
exec
/bin/zsh -lc "sed -n '1,220p' prompts/v2/system-simplify.md" in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 60ms:
# System Design Simplifier (v2) - The Minimalist

You are a **ruthless simplifier**. Your job is to take a system design and ask: "What's the simplest thing that could possibly work?"

## Your Philosophy

> "Perfection is achieved not when there is nothing more to add, but when there is nothing left to take away." — Antoine de Saint-Exupéry

Every component must justify its existence. If you can't articulate why removing it would break the system, remove it.

## Simplification Checklist

### 1. Component Audit
For each component, ask:
- [ ] What breaks if I remove this entirely?
- [ ] Can this be replaced with a managed service?
- [ ] Can this be merged with another component?
- [ ] Is this solving a problem we actually have?

### 2. The "Just Use X" Test
exec
/bin/zsh -lc 'ls scripts | head -n 50' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 44ms:
generate-solution-v2.sh
generate-solution.sh
grade-all-parallel.sh
grade-solution.sh
monitor.py
run-all-parallel.sh
run-all-v2-parallel.sh
run-batch.sh
run-pilot-batch.sh
validate-mermaid.mjs
validate-mermaid.sh
exec
/bin/zsh -lc "sed -n '1,200p' scripts/generate-solution-v2.sh" in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 50ms:
#!/bin/bash
# generate-solution-v2.sh - Generate solutions with elegance-focused pipeline
#
# Usage: ./scripts/generate-solution-v2.sh <slug> <title> <category> <category_dir> <description>
#
# Pipeline:
#   1. Generate: Create design with elegance mindset
#   2. Challenge: Constructive critic finds improvements
#   3. Refine: Incorporate feedback + simplify
#   4. Validate: Check Mermaid diagrams

set -e

SLUG="$1"
TITLE="$2"
CATEGORY="$3"
CATEGORY_DIR="$4"
DESCRIPTION="$5"

if [[ -z "$SLUG" || -z "$TITLE" || -z "$CATEGORY" || -z "$CATEGORY_DIR" || -z "$DESCRIPTION" ]]; then




thinking
**Simplifying component structures**

I want to avoid naming object storage as a component. Instead, I’ll say, “Rule Control Plane publishes signed artifacts to a static HTTPS endpoint, and POPs poll and cache.” That’s part of the control plane infrastructure. 

I’m considering merging the Analytics/Tuning component into Rule Control Plane since they’re part of the same system. I’m questioning if we can keep the Event Stream but leverage the existing logging, while still ensuring that telemetry is best-effort. I might decide to keep the Event Stream component as is.
thinking
**Defining Event Stream management**

I need to decide whether to keep Event Stream as managed or merge it into the control plane. If I merge, that removes the Event Stream component, which could lead to heavy ingestion at 1 million requests per second. Sampling and batching might help, but given our small team, it's easiest to use a managed log ingest without naming it. 

I think I can keep the Event Stream and reduce components to Edge Proxy, WAF Filter, Origin App, Event Stream, and Rule Control Plane. That's five components that’ll keep it manageable, but I’m considering potential failure scenarios too.
thinking
**Defining the WAF framework**

I want to outline that the WAF data plane holds the current artifact and checks signatures, which include the next poll interval. The distribution will use pull semantics with version pinning. To prevent split-brain scenarios, I’ll make sure rollouts freeze by default if the POP can’t report health.

For telemetry issues, I could implement a bounded queue and drop strategy. I’m also thinking about challenges requiring HMAC-signed cookies with TTL and key rotation. If a challenge fails, should I degrade to a `LOG_ONLY` action? I have to think carefully about these performance aspects without hedging or introducing ambiguity.
thinking
**Updating component documentation**

I need to ensure that each component includes justification for its necessity. I’ll create a section titled "What We Removed," listing components and complexity that have been eliminated. 

Next, I'll update the trade-offs table to reflect these changes. I should also maintain the operational notes to keep everything transparent. Finally, I’ll proceed to update the plan step statuses, confirming that step 1 is now complete. This helps in maintaining clarity and accountability throughout the process.
Plan update
  ✓ Locate existing WAF markdown file
  → Draft simplified design content
  • Update file and diagrams
  • Sanity-check markdown rendering






thinking
**Simplifying control plane design**

I’m exploring valid simplifications based on critique. I think I’ll start with the Envoy module and a pull-based distribution approach, using OWASP CRS semantics. While GitOps could add complexity, it also presents new workflows if we consider the control plane as a Git source of truth. I wonder if we can simplify further by dropping the UI entirely and focusing on a minimal API while still integrating GitOps. Let’s see how that works with our engineer resources for startup testing.
thinking
**Evaluating control plane features**

I'm considering the "Rule Control Plane" and whether to keep its API along with an optional UI. Should I remove anomaly scoring to simplify things? While it adds complexity, a limited scoring system could help avoid false positives. Perhaps we could use "shadow mode" and "per-rule mode" instead, which might suffice. However, I’m wondering if we really need anomaly scoring at all since explainability is easier with rules rather than scoring. Let's find that sweet spot!
thinking
**Refining WAF features**

I’m thinking about the simplest design for a Web Application Firewall (WAF). I want to focus on canonicalization, fixed substring matching, and safe regex; if there’s a high-confidence match, we block, otherwise allow. For weaker matches, we could use challenge or log_only options on a per-rule basis. I’m considering removing "anomaly scoring" as a separate concept and possibly integrating minimal scoring. I also want to merge analytics into the control plane and revise some requirements for better functionality. Now, I’m ready to implement this in the designated file!
thinking
**Drafting WAF documentation**

I’m preparing to include the fenced ` ```markdown` block at the top and bottom of my new file, ensuring that the Mermaid diagram is correctly placed within triple backticks. I’m considering running the mermaid validation script `scripts/validate-mermaid.sh` on this new file. Before I write anything, I need to carefully draft the content. I want to create a comprehensive document, including key insights on the Edge WAF, its role, and how it operates within the existing edge proxy architecture.
thinking
**Finalizing technical documentation**

I’m making sure to keep the document within a code fence wrapper by including ` ```markdown` at the top and bottom. I want to include a thorough overview of the data plane's responsibilities and its requirements, highlighting trade-offs between accuracy and latency. For the architecture, I’m emphasizing the relationships between key components like the Edge Proxy, WAF Filter, and Rule Control Plane. I also need to address failure modes, operational notes, and clarify what I’ve removed from the design. This will create a cohesive and informative document!
thinking
**Preparing WAF documentation validation**

I need to remember that adding components like object storage or CDN isn't allowed; however, I can reference the "managed event stream" since that's already part of my document. I'll take a look at other solutions-v2 docs to confirm their structure and make sure I follow suit. Now, it's time to create the new file at `solutions-v2/08-security-access-control/web-application-firewall.md` and run `scripts/validate-mermaid.sh` on it to ensure everything is validated properly for consistency!
exec
/bin/zsh -lc "sed -n '1,200p' scripts/validate-mermaid.sh" in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 43ms:
#!/bin/bash
# Validate Mermaid diagrams in markdown files
# Usage: ./scripts/validate-mermaid.sh [--fix]
# Run from project root or frontend directory

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"
FRONTEND_DIR="$PROJECT_DIR/frontend"

# Find mmdc
MMDC="$FRONTEND_DIR/node_modules/.bin/mmdc"
if [[ ! -x "$MMDC" ]]; then
    echo "Error: mermaid-cli not found. Run: cd frontend && npm install"
    exit 1
fi

SOLUTIONS_DIR="$PROJECT_DIR/solutions"
TEMP_DIR=$(mktemp -d)

thinking
**Checking validation scripts**

I’m realizing that the current script is set to validate the solutions directory but not the solutions-v2. This means that any new files in solutions-v2 won’t be checked by this script, which could be a problem. I’m wondering if there’s something like validate-mermaid.mjs that could help with this check. It might be worth digging into validate-mermaid.mjs to see how it works and if it can address this issue. Let's investigate!
exec
/bin/zsh -lc "sed -n '1,80p' scripts/validate-mermaid.mjs" in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 48ms:
#!/usr/bin/env node
/**
 * Validate Mermaid diagrams in markdown files
 * Run from frontend directory: node ../scripts/validate-mermaid.mjs
 * Or: cd frontend && node ../scripts/validate-mermaid.mjs [files...] [--fix]
 */

import { readFileSync, writeFileSync, readdirSync, statSync } from 'fs';
import { join, relative, dirname } from 'path';
import { fileURLToPath } from 'url';
import { createRequire } from 'module';

const __dirname = dirname(fileURLToPath(import.meta.url));
const require = createRequire(import.meta.url);

// We'll use mermaid's parse function via dynamic import
let mermaid;

function extractMermaidBlocks(content, filePath) {
  const blocks = [];

thinking
**Simplifying architecture design**

I need to remove "Rule Distributor" and simplify the architecture diagram accordingly. The goal is to keep things minimal. It's important to present stress-test scenarios like control plane failures, network partitions, and CPU spikes, while highlighting that the challenge is self-hosted and doesn't require a provider. We're saying that the challenge is served by the edge with an HMAC token, ensuring no external dependency. 

We’ll keep components like "Edge Proxy," "WAF Filter," and "Origin App," and merge Rule Distributor into Rule Control Plane. I'll also note that Edge Proxy uses Envoy/Nginx without adding extra components. Finally, we want to include key design decisions while keeping everything aligned with the original plan.
file update
A /Users/dominikdabrowski/pr/system-design-atlas/solutions-v2/08-security-access-control/web-application-firewall.md
```markdown
---
title: "Web Application Firewall (WAF)"
category: "Security & Access Control"
difficulty: "Hard"
tags: [security, edge, waf, http, ddos, rules, observability]
---

## Overview

This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.

The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.

## What Makes This Hard

1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
3. **Global blast radius:** one bad update can break every property faster than any application deploy.

## Requirements

### Functional Requirements
- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
- Provide shadow mode and explainability: stable rule IDs + matched locations.
- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
- Ensure observability cannot add latency: bounded queues + drop-on-overload.

### Scale Targets
- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
- **Latency budget:** +2 ms p99 added by WAF at the edge.
- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.

## Key Design Decisions

- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
  - Rejected: custom proxy stack or per-request scripting
  - Why: keep protocol handling boring and keep evaluation bounded.

- **Choose: signed, versioned rule artifacts pulled by POPs**
  - Rejected: push-based “distributor” dependencies in the hot path
  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.

- **Choose: bounded parsing and matching with runtime circuit breakers**
  - Rejected: “full inspection” by buffering and best-effort timeouts
  - Why: adversaries control input; the WAF must control worst-case work.

- **Choose: stateless challenges**
  - Rejected: global state stores or per-request lookups for challenges
  - Why: challenges must not introduce a new availability dependency.

## Architecture

```mermaid
flowchart LR
  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
  E --> O[Origin App]
  E --> L[Event Stream]
  CP[Rule Control Plane] --> E
  L --> CP
```

### Components

- **Edge Proxy**
  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.

- **WAF Filter (Data Plane)**
  - Why it exists: the only place decisions must be made per request with a strict latency budget.
  - Properties:
    - deterministic evaluation (no network calls)
    - strict caps (decoded bytes, token counts, match work)
    - verifies rule artifact signatures and hot-swaps by version
    - challenges are stateless (signed short-TTL token cookie)

- **Origin App**
  - Why it exists: the system being protected; WAF is additive.

- **Rule Control Plane**
  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
  - Responsibilities:
    - compile rules into bounded match primitives and reject unsafe constructs
    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
    - sign and publish immutable rule artifacts by version
    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version

- **Event Stream**
  - Why it exists: tuning and incident response without coupling telemetry to request latency.
  - Properties:
    - edge has a bounded queue; events drop when full
    - allows are sampled; blocks/challenges are prioritized
    - local counters/metrics always emitted even if detailed events drop

## Deep Dive: Line-Speed Detection Without Regex DoS

### 1) Canonicalization first, with a clear contract
The data plane normalizes inputs so matching is predictable:
- Percent-decoding with recursion limits and strict invalid-encoding rejection
- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
- Header/cookie parsing with bounded field counts and strict duplicate-header policy
- UTF-8 validation; invalid encodings are immediate block signals

Shadow mode is used to introduce stricter normalization safely (observe before enforce).

### 2) Compile rules into bounded-time primitives
- Multi-pattern scan for fixed substrings (Aho–Corasick class)
- Safe regex only (RE2-class; capped by inspected length and match work)
- Small scoring only where needed (weak signals add up; strong signals act alone)

### 3) Streaming body inspection with hard caps
- Only inspect bodies on routes that accept user input, up to a route byte budget.
- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
- Stop inspecting after the budget and continue forwarding the stream.

### 4) Bounded execution enforcement and safe rollouts
Every request has a fixed work budget:
- per-phase byte limits (decoded URL/headers/cookies/body)
- per-rule match work caps and a global per-request time budget
- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work

Every rollout is gated:
- artifacts are immutable, signed, and verified in the POP before activation
- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
- rollback is a version pin; POPs revert on next poll without needing new artifacts

## Trade-offs

| Optimized For | Sacrificed |
|--------------|------------|
| Predictable latency under attack | Deep inspection of huge/streaming bodies |
| Offline-safe edge operation | “Live” per-request policy decisions |
| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |

## Failure Modes

- **Bad ruleset blocks legitimate traffic**
  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.

- **Control plane down for minutes**
  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
  - Recover: restore control plane; no edge impact required.

- **Network partition between POPs and control plane**
  - What happens: POPs keep pinned version; upgrades freeze.
  - Recover: connectivity returns; rollouts resume from known version state.

- **Event stream slow or down**
  - What happens: edge drops events once queues fill; request latency unaffected.
  - Recover: telemetry returns; counters show the gap; no edge change required.

- **CPU saturation at POP during attack**
  - Detect: rising evaluation time, queue depth, p99.
  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.

## What We Removed

- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
- Unbounded matching constructs: only bounded primitives and safe regex.

## Operational Notes

- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
```
apply_patch(auto_approved=true) exited 0 in 20ms:
Success. Updated the following files:
A solutions-v2/08-security-access-control/web-application-firewall.md
file update:
diff --git a/solutions-v2/08-security-access-control/web-application-firewall.md b/solutions-v2/08-security-access-control/web-application-firewall.md
new file mode 100644
index 0000000000000000000000000000000000000000..82f500e2eb9cd7377a941bf0ec5280c3177cea62
--- /dev/null
+++ b/solutions-v2/08-security-access-control/web-application-firewall.md
@@ -0,0 +1,174 @@
+```markdown
+---
+title: "Web Application Firewall (WAF)"
+category: "Security & Access Control"
+difficulty: "Hard"
+tags: [security, edge, waf, http, ddos, rules, observability]
+---
+
+## Overview
+
+This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.
+
+The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.
+
+## What Makes This Hard
+
+1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
+2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
+3. **Global blast radius:** one bad update can break every property faster than any application deploy.
+
+## Requirements
+
+### Functional Requirements
+- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
+- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
+- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
+- Provide shadow mode and explainability: stable rule IDs + matched locations.
+- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
+- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
+- Ensure observability cannot add latency: bounded queues + drop-on-overload.
+
+### Scale Targets
+- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
+- **Latency budget:** +2 ms p99 added by WAF at the edge.
+- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
+- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.
+
+## Key Design Decisions
+
+- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
+  - Rejected: custom proxy stack or per-request scripting
+  - Why: keep protocol handling boring and keep evaluation bounded.
+
+- **Choose: signed, versioned rule artifacts pulled by POPs**
+  - Rejected: push-based “distributor” dependencies in the hot path
+  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.
+
+- **Choose: bounded parsing and matching with runtime circuit breakers**
+  - Rejected: “full inspection” by buffering and best-effort timeouts
+  - Why: adversaries control input; the WAF must control worst-case work.
+
+- **Choose: stateless challenges**
+  - Rejected: global state stores or per-request lookups for challenges
+  - Why: challenges must not introduce a new availability dependency.
+
+## Architecture
+
+```mermaid
+flowchart LR
+  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
+  E --> O[Origin App]
+  E --> L[Event Stream]
+  CP[Rule Control Plane] --> E
+  L --> CP
+```
+
+### Components
+
+- **Edge Proxy**
+  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
+  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.
+
+- **WAF Filter (Data Plane)**
+  - Why it exists: the only place decisions must be made per request with a strict latency budget.
+  - Properties:
+    - deterministic evaluation (no network calls)
+    - strict caps (decoded bytes, token counts, match work)
+    - verifies rule artifact signatures and hot-swaps by version
+    - challenges are stateless (signed short-TTL token cookie)
+
+- **Origin App**
+  - Why it exists: the system being protected; WAF is additive.
+
+- **Rule Control Plane**
+  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
+  - Responsibilities:
+    - compile rules into bounded match primitives and reject unsafe constructs
+    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
+    - sign and publish immutable rule artifacts by version
+    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version
+
+- **Event Stream**
+  - Why it exists: tuning and incident response without coupling telemetry to request latency.
+  - Properties:
+    - edge has a bounded queue; events drop when full
+    - allows are sampled; blocks/challenges are prioritized
+    - local counters/metrics always emitted even if detailed events drop
+
+## Deep Dive: Line-Speed Detection Without Regex DoS
+
+### 1) Canonicalization first, with a clear contract
+The data plane normalizes inputs so matching is predictable:
+- Percent-decoding with recursion limits and strict invalid-encoding rejection
+- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
+- Header/cookie parsing with bounded field counts and strict duplicate-header policy
+- UTF-8 validation; invalid encodings are immediate block signals
+
+Shadow mode is used to introduce stricter normalization safely (observe before enforce).
+
+### 2) Compile rules into bounded-time primitives
+- Multi-pattern scan for fixed substrings (Aho–Corasick class)
+- Safe regex only (RE2-class; capped by inspected length and match work)
+- Small scoring only where needed (weak signals add up; strong signals act alone)
+
+### 3) Streaming body inspection with hard caps
+- Only inspect bodies on routes that accept user input, up to a route byte budget.
+- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
+- Stop inspecting after the budget and continue forwarding the stream.
+
+### 4) Bounded execution enforcement and safe rollouts
+Every request has a fixed work budget:
+- per-phase byte limits (decoded URL/headers/cookies/body)
+- per-rule match work caps and a global per-request time budget
+- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work
+
+Every rollout is gated:
+- artifacts are immutable, signed, and verified in the POP before activation
+- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
+- rollback is a version pin; POPs revert on next poll without needing new artifacts
+
+## Trade-offs
+
+| Optimized For | Sacrificed |
+|--------------|------------|
+| Predictable latency under attack | Deep inspection of huge/streaming bodies |
+| Offline-safe edge operation | “Live” per-request policy decisions |
+| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
+| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |
+
+## Failure Modes
+
+- **Bad ruleset blocks legitimate traffic**
+  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
+  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.
+
+- **Control plane down for minutes**
+  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
+  - Recover: restore control plane; no edge impact required.
+
+- **Network partition between POPs and control plane**
+  - What happens: POPs keep pinned version; upgrades freeze.
+  - Recover: connectivity returns; rollouts resume from known version state.
+
+- **Event stream slow or down**
+  - What happens: edge drops events once queues fill; request latency unaffected.
+  - Recover: telemetry returns; counters show the gap; no edge change required.
+
+- **CPU saturation at POP during attack**
+  - Detect: rising evaluation time, queue depth, p99.
+  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.
+
+## What We Removed
+
+- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
+- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
+- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
+- Unbounded matching constructs: only bounded primitives and safe regex.
+
+## Operational Notes
+
+- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
+- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
+- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
+```

file update:
diff --git a/solutions-v2/08-security-access-control/web-application-firewall.md b/solutions-v2/08-security-access-control/web-application-firewall.md
new file mode 100644
index 0000000000000000000000000000000000000000..82f500e2eb9cd7377a941bf0ec5280c3177cea62
--- /dev/null
+++ b/solutions-v2/08-security-access-control/web-application-firewall.md
@@ -0,0 +1,174 @@
+```markdown
+---
+title: "Web Application Firewall (WAF)"
+category: "Security & Access Control"
+difficulty: "Hard"
+tags: [security, edge, waf, http, ddos, rules, observability]
+---
+
+## Overview
+
+This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.
+
+The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.
+
+## What Makes This Hard
+
+1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
+2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
+3. **Global blast radius:** one bad update can break every property faster than any application deploy.
+
+## Requirements
+
+### Functional Requirements
+- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
+- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
+- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
+- Provide shadow mode and explainability: stable rule IDs + matched locations.
+- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
+- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
+- Ensure observability cannot add latency: bounded queues + drop-on-overload.
+
+### Scale Targets
+- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
+- **Latency budget:** +2 ms p99 added by WAF at the edge.
+- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
+- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.
+
+## Key Design Decisions
+
+- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
+  - Rejected: custom proxy stack or per-request scripting
+  - Why: keep protocol handling boring and keep evaluation bounded.
+
+- **Choose: signed, versioned rule artifacts pulled by POPs**
+  - Rejected: push-based “distributor” dependencies in the hot path
+  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.
+
+- **Choose: bounded parsing and matching with runtime circuit breakers**
+  - Rejected: “full inspection” by buffering and best-effort timeouts
+  - Why: adversaries control input; the WAF must control worst-case work.
+
+- **Choose: stateless challenges**
+  - Rejected: global state stores or per-request lookups for challenges
+  - Why: challenges must not introduce a new availability dependency.
+
+## Architecture
+
+```mermaid
+flowchart LR
+  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
+  E --> O[Origin App]
+  E --> L[Event Stream]
+  CP[Rule Control Plane] --> E
+  L --> CP
+```
+
+### Components
+
+- **Edge Proxy**
+  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
+  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.
+
+- **WAF Filter (Data Plane)**
+  - Why it exists: the only place decisions must be made per request with a strict latency budget.
+  - Properties:
+    - deterministic evaluation (no network calls)
+    - strict caps (decoded bytes, token counts, match work)
+    - verifies rule artifact signatures and hot-swaps by version
+    - challenges are stateless (signed short-TTL token cookie)
+
+- **Origin App**
+  - Why it exists: the system being protected; WAF is additive.
+
+- **Rule Control Plane**
+  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
+  - Responsibilities:
+    - compile rules into bounded match primitives and reject unsafe constructs
+    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
+    - sign and publish immutable rule artifacts by version
+    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version
+
+- **Event Stream**
+  - Why it exists: tuning and incident response without coupling telemetry to request latency.
+  - Properties:
+    - edge has a bounded queue; events drop when full
+    - allows are sampled; blocks/challenges are prioritized
+    - local counters/metrics always emitted even if detailed events drop
+
+## Deep Dive: Line-Speed Detection Without Regex DoS
+
+### 1) Canonicalization first, with a clear contract
+The data plane normalizes inputs so matching is predictable:
+- Percent-decoding with recursion limits and strict invalid-encoding rejection
+- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
+- Header/cookie parsing with bounded field counts and strict duplicate-header policy
+- UTF-8 validation; invalid encodings are immediate block signals
+
+Shadow mode is used to introduce stricter normalization safely (observe before enforce).
+
+### 2) Compile rules into bounded-time primitives
+- Multi-pattern scan for fixed substrings (Aho–Corasick class)
+- Safe regex only (RE2-class; capped by inspected length and match work)
+- Small scoring only where needed (weak signals add up; strong signals act alone)
+
+### 3) Streaming body inspection with hard caps
+- Only inspect bodies on routes that accept user input, up to a route byte budget.
+- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
+- Stop inspecting after the budget and continue forwarding the stream.
+
+### 4) Bounded execution enforcement and safe rollouts
+Every request has a fixed work budget:
+- per-phase byte limits (decoded URL/headers/cookies/body)
+- per-rule match work caps and a global per-request time budget
+- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work
+
+Every rollout is gated:
+- artifacts are immutable, signed, and verified in the POP before activation
+- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
+- rollback is a version pin; POPs revert on next poll without needing new artifacts
+
+## Trade-offs
+
+| Optimized For | Sacrificed |
+|--------------|------------|
+| Predictable latency under attack | Deep inspection of huge/streaming bodies |
+| Offline-safe edge operation | “Live” per-request policy decisions |
+| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
+| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |
+
+## Failure Modes
+
+- **Bad ruleset blocks legitimate traffic**
+  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
+  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.
+
+- **Control plane down for minutes**
+  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
+  - Recover: restore control plane; no edge impact required.
+
+- **Network partition between POPs and control plane**
+  - What happens: POPs keep pinned version; upgrades freeze.
+  - Recover: connectivity returns; rollouts resume from known version state.
+
+- **Event stream slow or down**
+  - What happens: edge drops events once queues fill; request latency unaffected.
+  - Recover: telemetry returns; counters show the gap; no edge change required.
+
+- **CPU saturation at POP during attack**
+  - Detect: rising evaluation time, queue depth, p99.
+  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.
+
+## What We Removed
+
+- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
+- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
+- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
+- Unbounded matching constructs: only bounded primitives and safe regex.
+
+## Operational Notes
+
+- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
+- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
+- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
+```

Plan update
  ✓ Locate existing WAF markdown file
  ✓ Draft simplified design content
  → Update file and diagrams
  • Sanity-check markdown rendering


thinking
**Verifying file updates**

I’m making sure we’ve updated the file and diagrams correctly. I need to verify the validity of our mermaid diagram and ensure we didn’t add any components. We removed certain elements, but I noticed the control plane is connected directly to the edge, which might imply pushing when we meant pulling. I also have to check that the file path includes the correct slug name, which is "web-application-firewall." Lastly, I must confirm that all tags, difficulty levels, and sections are correct and unchanged.
thinking
**Finalizing document changes**

I’ve replaced the old section with a new one, and I didn’t add any new top-level sections besides that. However, we removed several items, including "What Makes This Hard," "Requirements," and others, keeping it consistent with the original, which is good. The content regarding failure modes was slightly expanded, but that’s fine. For the next step, I’ll ensure the diagrams reflect the simplifications. I need to verify the markdown rendering, validating the file with a Node script, and check if the necessary modules are present in the frontend directory.
file update:
diff --git a/solutions-v2/08-security-access-control/web-application-firewall.md b/solutions-v2/08-security-access-control/web-application-firewall.md
new file mode 100644
index 0000000000000000000000000000000000000000..82f500e2eb9cd7377a941bf0ec5280c3177cea62
--- /dev/null
+++ b/solutions-v2/08-security-access-control/web-application-firewall.md
@@ -0,0 +1,174 @@
+```markdown
+---
+title: "Web Application Firewall (WAF)"
+category: "Security & Access Control"
+difficulty: "Hard"
+tags: [security, edge, waf, http, ddos, rules, observability]
+---
+
+## Overview
+
+This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.
+
+The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.
+
+## What Makes This Hard
+
+1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
+2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
+3. **Global blast radius:** one bad update can break every property faster than any application deploy.
+
+## Requirements
+
+### Functional Requirements
+- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
+- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
+- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
+- Provide shadow mode and explainability: stable rule IDs + matched locations.
+- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
+- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
+- Ensure observability cannot add latency: bounded queues + drop-on-overload.
+
+### Scale Targets
+- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
+- **Latency budget:** +2 ms p99 added by WAF at the edge.
+- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
+- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.
+
+## Key Design Decisions
+
+- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
+  - Rejected: custom proxy stack or per-request scripting
+  - Why: keep protocol handling boring and keep evaluation bounded.
+
+- **Choose: signed, versioned rule artifacts pulled by POPs**
+  - Rejected: push-based “distributor” dependencies in the hot path
+  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.
+
+- **Choose: bounded parsing and matching with runtime circuit breakers**
+  - Rejected: “full inspection” by buffering and best-effort timeouts
+  - Why: adversaries control input; the WAF must control worst-case work.
+
+- **Choose: stateless challenges**
+  - Rejected: global state stores or per-request lookups for challenges
+  - Why: challenges must not introduce a new availability dependency.
+
+## Architecture
+
+```mermaid
+flowchart LR
+  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
+  E --> O[Origin App]
+  E --> L[Event Stream]
+  CP[Rule Control Plane] --> E
+  L --> CP
+```
+
+### Components
+
+- **Edge Proxy**
+  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
+  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.
+
+- **WAF Filter (Data Plane)**
+  - Why it exists: the only place decisions must be made per request with a strict latency budget.
+  - Properties:
+    - deterministic evaluation (no network calls)
+    - strict caps (decoded bytes, token counts, match work)
+    - verifies rule artifact signatures and hot-swaps by version
+    - challenges are stateless (signed short-TTL token cookie)
+
+- **Origin App**
+  - Why it exists: the system being protected; WAF is additive.
+
+- **Rule Control Plane**
+  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
+  - Responsibilities:
+    - compile rules into bounded match primitives and reject unsafe constructs
+    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
+    - sign and publish immutable rule artifacts by version
+    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version
+
+- **Event Stream**
+  - Why it exists: tuning and incident response without coupling telemetry to request latency.
+  - Properties:
+    - edge has a bounded queue; events drop when full
+    - allows are sampled; blocks/challenges are prioritized
+    - local counters/metrics always emitted even if detailed events drop
+
+## Deep Dive: Line-Speed Detection Without Regex DoS
+
+### 1) Canonicalization first, with a clear contract
+The data plane normalizes inputs so matching is predictable:
+- Percent-decoding with recursion limits and strict invalid-encoding rejection
+- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
+- Header/cookie parsing with bounded field counts and strict duplicate-header policy
+- UTF-8 validation; invalid encodings are immediate block signals
+
+Shadow mode is used to introduce stricter normalization safely (observe before enforce).
+
+### 2) Compile rules into bounded-time primitives
+- Multi-pattern scan for fixed substrings (Aho–Corasick class)
+- Safe regex only (RE2-class; capped by inspected length and match work)
+- Small scoring only where needed (weak signals add up; strong signals act alone)
+
+### 3) Streaming body inspection with hard caps
+- Only inspect bodies on routes that accept user input, up to a route byte budget.
+- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
+- Stop inspecting after the budget and continue forwarding the stream.
+
+### 4) Bounded execution enforcement and safe rollouts
+Every request has a fixed work budget:
+- per-phase byte limits (decoded URL/headers/cookies/body)
+- per-rule match work caps and a global per-request time budget
+- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work
+
+Every rollout is gated:
+- artifacts are immutable, signed, and verified in the POP before activation
+- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
+- rollback is a version pin; POPs revert on next poll without needing new artifacts
+
+## Trade-offs
+
+| Optimized For | Sacrificed |
+|--------------|------------|
+| Predictable latency under attack | Deep inspection of huge/streaming bodies |
+| Offline-safe edge operation | “Live” per-request policy decisions |
+| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
+| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |
+
+## Failure Modes
+
+- **Bad ruleset blocks legitimate traffic**
+  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
+  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.
+
+- **Control plane down for minutes**
+  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
+  - Recover: restore control plane; no edge impact required.
+
+- **Network partition between POPs and control plane**
+  - What happens: POPs keep pinned version; upgrades freeze.
+  - Recover: connectivity returns; rollouts resume from known version state.
+
+- **Event stream slow or down**
+  - What happens: edge drops events once queues fill; request latency unaffected.
+  - Recover: telemetry returns; counters show the gap; no edge change required.
+
+- **CPU saturation at POP during attack**
+  - Detect: rising evaluation time, queue depth, p99.
+  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.
+
+## What We Removed
+
+- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
+- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
+- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
+- Unbounded matching constructs: only bounded primitives and safe regex.
+
+## Operational Notes
+
+- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
+- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
+- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
+```

exec
/bin/zsh -lc 'ls frontend/node_modules/.bin | head' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 50ms:
acorn
astro
astro-check
astro-ls
autoprefixer
baseline-browser-mapping
browsers
browserslist
cssesc
csv2json

thinking
**Running validation script**

I see that there’s a `node_modules` folder, which is a good sign! I plan to run the `validate-mermaid.mjs` script from the frontend directory. The command would typically involve changing directories, but the tool setting advises against that. Instead, I’ll set the working directory to frontend and run the command like this: `node ../scripts/validate-mermaid.mjs ../solutions-v2/08-security-access-control/web-application-firewall.md`. This should work just fine!
file update:
diff --git a/solutions-v2/08-security-access-control/web-application-firewall.md b/solutions-v2/08-security-access-control/web-application-firewall.md
new file mode 100644
index 0000000000000000000000000000000000000000..82f500e2eb9cd7377a941bf0ec5280c3177cea62
--- /dev/null
+++ b/solutions-v2/08-security-access-control/web-application-firewall.md
@@ -0,0 +1,174 @@
+```markdown
+---
+title: "Web Application Firewall (WAF)"
+category: "Security & Access Control"
+difficulty: "Hard"
+tags: [security, edge, waf, http, ddos, rules, observability]
+---
+
+## Overview
+
+This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.
+
+The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.
+
+## What Makes This Hard
+
+1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
+2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
+3. **Global blast radius:** one bad update can break every property faster than any application deploy.
+
+## Requirements
+
+### Functional Requirements
+- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
+- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
+- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
+- Provide shadow mode and explainability: stable rule IDs + matched locations.
+- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
+- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
+- Ensure observability cannot add latency: bounded queues + drop-on-overload.
+
+### Scale Targets
+- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
+- **Latency budget:** +2 ms p99 added by WAF at the edge.
+- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
+- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.
+
+## Key Design Decisions
+
+- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
+  - Rejected: custom proxy stack or per-request scripting
+  - Why: keep protocol handling boring and keep evaluation bounded.
+
+- **Choose: signed, versioned rule artifacts pulled by POPs**
+  - Rejected: push-based “distributor” dependencies in the hot path
+  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.
+
+- **Choose: bounded parsing and matching with runtime circuit breakers**
+  - Rejected: “full inspection” by buffering and best-effort timeouts
+  - Why: adversaries control input; the WAF must control worst-case work.
+
+- **Choose: stateless challenges**
+  - Rejected: global state stores or per-request lookups for challenges
+  - Why: challenges must not introduce a new availability dependency.
+
+## Architecture
+
+```mermaid
+flowchart LR
+  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
+  E --> O[Origin App]
+  E --> L[Event Stream]
+  CP[Rule Control Plane] --> E
+  L --> CP
+```
+
+### Components
+
+- **Edge Proxy**
+  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
+  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.
+
+- **WAF Filter (Data Plane)**
+  - Why it exists: the only place decisions must be made per request with a strict latency budget.
+  - Properties:
+    - deterministic evaluation (no network calls)
+    - strict caps (decoded bytes, token counts, match work)
+    - verifies rule artifact signatures and hot-swaps by version
+    - challenges are stateless (signed short-TTL token cookie)
+
+- **Origin App**
+  - Why it exists: the system being protected; WAF is additive.
+
+- **Rule Control Plane**
+  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
+  - Responsibilities:
+    - compile rules into bounded match primitives and reject unsafe constructs
+    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
+    - sign and publish immutable rule artifacts by version
+    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version
+
+- **Event Stream**
+  - Why it exists: tuning and incident response without coupling telemetry to request latency.
+  - Properties:
+    - edge has a bounded queue; events drop when full
+    - allows are sampled; blocks/challenges are prioritized
+    - local counters/metrics always emitted even if detailed events drop
+
+## Deep Dive: Line-Speed Detection Without Regex DoS
+
+### 1) Canonicalization first, with a clear contract
+The data plane normalizes inputs so matching is predictable:
+- Percent-decoding with recursion limits and strict invalid-encoding rejection
+- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
+- Header/cookie parsing with bounded field counts and strict duplicate-header policy
+- UTF-8 validation; invalid encodings are immediate block signals
+
+Shadow mode is used to introduce stricter normalization safely (observe before enforce).
+
+### 2) Compile rules into bounded-time primitives
+- Multi-pattern scan for fixed substrings (Aho–Corasick class)
+- Safe regex only (RE2-class; capped by inspected length and match work)
+- Small scoring only where needed (weak signals add up; strong signals act alone)
+
+### 3) Streaming body inspection with hard caps
+- Only inspect bodies on routes that accept user input, up to a route byte budget.
+- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
+- Stop inspecting after the budget and continue forwarding the stream.
+
+### 4) Bounded execution enforcement and safe rollouts
+Every request has a fixed work budget:
+- per-phase byte limits (decoded URL/headers/cookies/body)
+- per-rule match work caps and a global per-request time budget
+- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work
+
+Every rollout is gated:
+- artifacts are immutable, signed, and verified in the POP before activation
+- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
+- rollback is a version pin; POPs revert on next poll without needing new artifacts
+
+## Trade-offs
+
+| Optimized For | Sacrificed |
+|--------------|------------|
+| Predictable latency under attack | Deep inspection of huge/streaming bodies |
+| Offline-safe edge operation | “Live” per-request policy decisions |
+| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
+| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |
+
+## Failure Modes
+
+- **Bad ruleset blocks legitimate traffic**
+  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
+  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.
+
+- **Control plane down for minutes**
+  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
+  - Recover: restore control plane; no edge impact required.
+
+- **Network partition between POPs and control plane**
+  - What happens: POPs keep pinned version; upgrades freeze.
+  - Recover: connectivity returns; rollouts resume from known version state.
+
+- **Event stream slow or down**
+  - What happens: edge drops events once queues fill; request latency unaffected.
+  - Recover: telemetry returns; counters show the gap; no edge change required.
+
+- **CPU saturation at POP during attack**
+  - Detect: rising evaluation time, queue depth, p99.
+  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.
+
+## What We Removed
+
+- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
+- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
+- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
+- Unbounded matching constructs: only bounded primitives and safe regex.
+
+## Operational Notes
+
+- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
+- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
+- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
+```

exec
/bin/zsh -lc 'node ../scripts/validate-mermaid.mjs ../solutions-v2/08-security-access-control/web-application-firewall.md' in /Users/dominikdabrowski/pr/system-design-atlas/frontend exited 1 in 82ms:
Validation failed: Cannot find package 'mermaid' imported from /Users/dominikdabrowski/pr/system-design-atlas/scripts/validate-mermaid.mjs

thinking
**Checking Mermaid installation**

I notice that the Mermaid package isn’t installed, but that might not be a problem. There's a chance we can use the mmdc CLI. It seems like the script validate-mermaid.sh is looking for mmdc in the frontend/node_modules/.bin directory, but I need to confirm if the node_modules actually includes mermaid-cli. I’ll check whether `mmdc` exists to see if we can proceed.
file update:
diff --git a/solutions-v2/08-security-access-control/web-application-firewall.md b/solutions-v2/08-security-access-control/web-application-firewall.md
new file mode 100644
index 0000000000000000000000000000000000000000..82f500e2eb9cd7377a941bf0ec5280c3177cea62
--- /dev/null
+++ b/solutions-v2/08-security-access-control/web-application-firewall.md
@@ -0,0 +1,174 @@
+```markdown
+---
+title: "Web Application Firewall (WAF)"
+category: "Security & Access Control"
+difficulty: "Hard"
+tags: [security, edge, waf, http, ddos, rules, observability]
+---
+
+## Overview
+
+This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.
+
+The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.
+
+## What Makes This Hard
+
+1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
+2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
+3. **Global blast radius:** one bad update can break every property faster than any application deploy.
+
+## Requirements
+
+### Functional Requirements
+- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
+- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
+- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
+- Provide shadow mode and explainability: stable rule IDs + matched locations.
+- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
+- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
+- Ensure observability cannot add latency: bounded queues + drop-on-overload.
+
+### Scale Targets
+- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
+- **Latency budget:** +2 ms p99 added by WAF at the edge.
+- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
+- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.
+
+## Key Design Decisions
+
+- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
+  - Rejected: custom proxy stack or per-request scripting
+  - Why: keep protocol handling boring and keep evaluation bounded.
+
+- **Choose: signed, versioned rule artifacts pulled by POPs**
+  - Rejected: push-based “distributor” dependencies in the hot path
+  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.
+
+- **Choose: bounded parsing and matching with runtime circuit breakers**
+  - Rejected: “full inspection” by buffering and best-effort timeouts
+  - Why: adversaries control input; the WAF must control worst-case work.
+
+- **Choose: stateless challenges**
+  - Rejected: global state stores or per-request lookups for challenges
+  - Why: challenges must not introduce a new availability dependency.
+
+## Architecture
+
+```mermaid
+flowchart LR
+  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
+  E --> O[Origin App]
+  E --> L[Event Stream]
+  CP[Rule Control Plane] --> E
+  L --> CP
+```
+
+### Components
+
+- **Edge Proxy**
+  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
+  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.
+
+- **WAF Filter (Data Plane)**
+  - Why it exists: the only place decisions must be made per request with a strict latency budget.
+  - Properties:
+    - deterministic evaluation (no network calls)
+    - strict caps (decoded bytes, token counts, match work)
+    - verifies rule artifact signatures and hot-swaps by version
+    - challenges are stateless (signed short-TTL token cookie)
+
+- **Origin App**
+  - Why it exists: the system being protected; WAF is additive.
+
+- **Rule Control Plane**
+  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
+  - Responsibilities:
+    - compile rules into bounded match primitives and reject unsafe constructs
+    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
+    - sign and publish immutable rule artifacts by version
+    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version
+
+- **Event Stream**
+  - Why it exists: tuning and incident response without coupling telemetry to request latency.
+  - Properties:
+    - edge has a bounded queue; events drop when full
+    - allows are sampled; blocks/challenges are prioritized
+    - local counters/metrics always emitted even if detailed events drop
+
+## Deep Dive: Line-Speed Detection Without Regex DoS
+
+### 1) Canonicalization first, with a clear contract
+The data plane normalizes inputs so matching is predictable:
+- Percent-decoding with recursion limits and strict invalid-encoding rejection
+- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
+- Header/cookie parsing with bounded field counts and strict duplicate-header policy
+- UTF-8 validation; invalid encodings are immediate block signals
+
+Shadow mode is used to introduce stricter normalization safely (observe before enforce).
+
+### 2) Compile rules into bounded-time primitives
+- Multi-pattern scan for fixed substrings (Aho–Corasick class)
+- Safe regex only (RE2-class; capped by inspected length and match work)
+- Small scoring only where needed (weak signals add up; strong signals act alone)
+
+### 3) Streaming body inspection with hard caps
+- Only inspect bodies on routes that accept user input, up to a route byte budget.
+- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
+- Stop inspecting after the budget and continue forwarding the stream.
+
+### 4) Bounded execution enforcement and safe rollouts
+Every request has a fixed work budget:
+- per-phase byte limits (decoded URL/headers/cookies/body)
+- per-rule match work caps and a global per-request time budget
+- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work
+
+Every rollout is gated:
+- artifacts are immutable, signed, and verified in the POP before activation
+- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
+- rollback is a version pin; POPs revert on next poll without needing new artifacts
+
+## Trade-offs
+
+| Optimized For | Sacrificed |
+|--------------|------------|
+| Predictable latency under attack | Deep inspection of huge/streaming bodies |
+| Offline-safe edge operation | “Live” per-request policy decisions |
+| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
+| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |
+
+## Failure Modes
+
+- **Bad ruleset blocks legitimate traffic**
+  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
+  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.
+
+- **Control plane down for minutes**
+  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
+  - Recover: restore control plane; no edge impact required.
+
+- **Network partition between POPs and control plane**
+  - What happens: POPs keep pinned version; upgrades freeze.
+  - Recover: connectivity returns; rollouts resume from known version state.
+
+- **Event stream slow or down**
+  - What happens: edge drops events once queues fill; request latency unaffected.
+  - Recover: telemetry returns; counters show the gap; no edge change required.
+
+- **CPU saturation at POP during attack**
+  - Detect: rising evaluation time, queue depth, p99.
+  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.
+
+## What We Removed
+
+- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
+- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
+- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
+- Unbounded matching constructs: only bounded primitives and safe regex.
+
+## Operational Notes
+
+- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
+- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
+- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
+```

exec
/bin/zsh -lc 'test -x frontend/node_modules/.bin/mmdc && echo mmdc_yes || echo mmdc_no' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 46ms:
mmdc_yes
file update:
diff --git a/solutions-v2/08-security-access-control/web-application-firewall.md b/solutions-v2/08-security-access-control/web-application-firewall.md
new file mode 100644
index 0000000000000000000000000000000000000000..82f500e2eb9cd7377a941bf0ec5280c3177cea62
--- /dev/null
+++ b/solutions-v2/08-security-access-control/web-application-firewall.md
@@ -0,0 +1,174 @@
+```markdown
+---
+title: "Web Application Firewall (WAF)"
+category: "Security & Access Control"
+difficulty: "Hard"
+tags: [security, edge, waf, http, ddos, rules, observability]
+---
+
+## Overview
+
+This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.
+
+The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.
+
+## What Makes This Hard
+
+1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
+2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
+3. **Global blast radius:** one bad update can break every property faster than any application deploy.
+
+## Requirements
+
+### Functional Requirements
+- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
+- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
+- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
+- Provide shadow mode and explainability: stable rule IDs + matched locations.
+- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
+- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
+- Ensure observability cannot add latency: bounded queues + drop-on-overload.
+
+### Scale Targets
+- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
+- **Latency budget:** +2 ms p99 added by WAF at the edge.
+- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
+- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.
+
+## Key Design Decisions
+
+- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
+  - Rejected: custom proxy stack or per-request scripting
+  - Why: keep protocol handling boring and keep evaluation bounded.
+
+- **Choose: signed, versioned rule artifacts pulled by POPs**
+  - Rejected: push-based “distributor” dependencies in the hot path
+  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.
+
+- **Choose: bounded parsing and matching with runtime circuit breakers**
+  - Rejected: “full inspection” by buffering and best-effort timeouts
+  - Why: adversaries control input; the WAF must control worst-case work.
+
+- **Choose: stateless challenges**
+  - Rejected: global state stores or per-request lookups for challenges
+  - Why: challenges must not introduce a new availability dependency.
+
+## Architecture
+
+```mermaid
+flowchart LR
+  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
+  E --> O[Origin App]
+  E --> L[Event Stream]
+  CP[Rule Control Plane] --> E
+  L --> CP
+```
+
+### Components
+
+- **Edge Proxy**
+  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
+  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.
+
+- **WAF Filter (Data Plane)**
+  - Why it exists: the only place decisions must be made per request with a strict latency budget.
+  - Properties:
+    - deterministic evaluation (no network calls)
+    - strict caps (decoded bytes, token counts, match work)
+    - verifies rule artifact signatures and hot-swaps by version
+    - challenges are stateless (signed short-TTL token cookie)
+
+- **Origin App**
+  - Why it exists: the system being protected; WAF is additive.
+
+- **Rule Control Plane**
+  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
+  - Responsibilities:
+    - compile rules into bounded match primitives and reject unsafe constructs
+    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
+    - sign and publish immutable rule artifacts by version
+    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version
+
+- **Event Stream**
+  - Why it exists: tuning and incident response without coupling telemetry to request latency.
+  - Properties:
+    - edge has a bounded queue; events drop when full
+    - allows are sampled; blocks/challenges are prioritized
+    - local counters/metrics always emitted even if detailed events drop
+
+## Deep Dive: Line-Speed Detection Without Regex DoS
+
+### 1) Canonicalization first, with a clear contract
+The data plane normalizes inputs so matching is predictable:
+- Percent-decoding with recursion limits and strict invalid-encoding rejection
+- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
+- Header/cookie parsing with bounded field counts and strict duplicate-header policy
+- UTF-8 validation; invalid encodings are immediate block signals
+
+Shadow mode is used to introduce stricter normalization safely (observe before enforce).
+
+### 2) Compile rules into bounded-time primitives
+- Multi-pattern scan for fixed substrings (Aho–Corasick class)
+- Safe regex only (RE2-class; capped by inspected length and match work)
+- Small scoring only where needed (weak signals add up; strong signals act alone)
+
+### 3) Streaming body inspection with hard caps
+- Only inspect bodies on routes that accept user input, up to a route byte budget.
+- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
+- Stop inspecting after the budget and continue forwarding the stream.
+
+### 4) Bounded execution enforcement and safe rollouts
+Every request has a fixed work budget:
+- per-phase byte limits (decoded URL/headers/cookies/body)
+- per-rule match work caps and a global per-request time budget
+- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work
+
+Every rollout is gated:
+- artifacts are immutable, signed, and verified in the POP before activation
+- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
+- rollback is a version pin; POPs revert on next poll without needing new artifacts
+
+## Trade-offs
+
+| Optimized For | Sacrificed |
+|--------------|------------|
+| Predictable latency under attack | Deep inspection of huge/streaming bodies |
+| Offline-safe edge operation | “Live” per-request policy decisions |
+| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
+| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |
+
+## Failure Modes
+
+- **Bad ruleset blocks legitimate traffic**
+  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
+  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.
+
+- **Control plane down for minutes**
+  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
+  - Recover: restore control plane; no edge impact required.
+
+- **Network partition between POPs and control plane**
+  - What happens: POPs keep pinned version; upgrades freeze.
+  - Recover: connectivity returns; rollouts resume from known version state.
+
+- **Event stream slow or down**
+  - What happens: edge drops events once queues fill; request latency unaffected.
+  - Recover: telemetry returns; counters show the gap; no edge change required.
+
+- **CPU saturation at POP during attack**
+  - Detect: rising evaluation time, queue depth, p99.
+  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.
+
+## What We Removed
+
+- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
+- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
+- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
+- Unbounded matching constructs: only bounded primitives and safe regex.
+
+## Operational Notes
+
+- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
+- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
+- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
+```

exec
/bin/zsh -lc "awk 'BEGIN{in=0} /"'^```mermaid/{in=1;next} /''^```/{if(in){exit}} {if(in)print}'"' solutions-v2/08-security-access-control/web-application-firewall.md > .tmp-waf.mmd && frontend/node_modules/.bin/mmdc -i .tmp-waf.mmd -o .tmp-waf.svg >/dev/null 2>&1 && echo mermaid_ok || (echo mermaid_bad; frontend/node_modules/.bin/mmdc -i .tmp-waf.mmd -o .tmp-waf.svg)" in /Users/dominikdabrowski/pr/system-design-atlas exited 1 in 210ms:
awk: syntax error at source line 1
 context is
	 >>> BEGIN{in <<< =0} /^```mermaid/{in=1;next} /^```/{if(in){exit}} {if(in)print}
awk: illegal statement at source line 1
mermaid_bad
Generating single mermaid chart

Error: Failed to launch the browser process!
[1217/013253.277963:ERROR:thermal_state_observer_mac.mm(88)] ThermalStateObserverMac unable to register to power notifications. Result: 9
[1217/013253.279261:ERROR:dns_config_service_posix.cc(142)] DNS config watch failed to start.
[1217/013253.280617:WARNING:dns_config_service_posix.cc(201)] Failed to read DnsConfig.
[1217/013253.281335:FATAL:sandbox_parameters_mac.mm(68)] Check failed: . : Input/output error (5)


TROUBLESHOOTING: https://pptr.dev/troubleshooting

    at Interface.onClose (file:///Users/dominikdabrowski/pr/system-design-atlas/frontend/node_modules/@puppeteer/browsers/lib/esm/launch.js:303:24)
    at Interface.emit (node:events:536:35)
    at Interface.close (node:internal/readline/interface:527:10)
    at Socket.onend (node:internal/readline/interface:253:10)

thinking
**Resolving technical issues**

I’m facing some syntax issues with awk since 'in' is a reserved keyword. I think I’ll need to use a different variable name, maybe 'inside.' There's also a problem with mmdc failing due to the browser process in this sandbox environment. It can’t launch Chromium. I’m considering using 'mermaid' for a lightweight syntax check instead, but it seems the mermaid package is missing. I may still need to ensure the diagram is simple and clean up any temporary files created.
file update:
diff --git a/solutions-v2/08-security-access-control/web-application-firewall.md b/solutions-v2/08-security-access-control/web-application-firewall.md
new file mode 100644
index 0000000000000000000000000000000000000000..82f500e2eb9cd7377a941bf0ec5280c3177cea62
--- /dev/null
+++ b/solutions-v2/08-security-access-control/web-application-firewall.md
@@ -0,0 +1,174 @@
+```markdown
+---
+title: "Web Application Firewall (WAF)"
+category: "Security & Access Control"
+difficulty: "Hard"
+tags: [security, edge, waf, http, ddos, rules, observability]
+---
+
+## Overview
+
+This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.
+
+The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.
+
+## What Makes This Hard
+
+1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
+2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
+3. **Global blast radius:** one bad update can break every property faster than any application deploy.
+
+## Requirements
+
+### Functional Requirements
+- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
+- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
+- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
+- Provide shadow mode and explainability: stable rule IDs + matched locations.
+- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
+- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
+- Ensure observability cannot add latency: bounded queues + drop-on-overload.
+
+### Scale Targets
+- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
+- **Latency budget:** +2 ms p99 added by WAF at the edge.
+- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
+- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.
+
+## Key Design Decisions
+
+- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
+  - Rejected: custom proxy stack or per-request scripting
+  - Why: keep protocol handling boring and keep evaluation bounded.
+
+- **Choose: signed, versioned rule artifacts pulled by POPs**
+  - Rejected: push-based “distributor” dependencies in the hot path
+  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.
+
+- **Choose: bounded parsing and matching with runtime circuit breakers**
+  - Rejected: “full inspection” by buffering and best-effort timeouts
+  - Why: adversaries control input; the WAF must control worst-case work.
+
+- **Choose: stateless challenges**
+  - Rejected: global state stores or per-request lookups for challenges
+  - Why: challenges must not introduce a new availability dependency.
+
+## Architecture
+
+```mermaid
+flowchart LR
+  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
+  E --> O[Origin App]
+  E --> L[Event Stream]
+  CP[Rule Control Plane] --> E
+  L --> CP
+```
+
+### Components
+
+- **Edge Proxy**
+  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
+  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.
+
+- **WAF Filter (Data Plane)**
+  - Why it exists: the only place decisions must be made per request with a strict latency budget.
+  - Properties:
+    - deterministic evaluation (no network calls)
+    - strict caps (decoded bytes, token counts, match work)
+    - verifies rule artifact signatures and hot-swaps by version
+    - challenges are stateless (signed short-TTL token cookie)
+
+- **Origin App**
+  - Why it exists: the system being protected; WAF is additive.
+
+- **Rule Control Plane**
+  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
+  - Responsibilities:
+    - compile rules into bounded match primitives and reject unsafe constructs
+    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
+    - sign and publish immutable rule artifacts by version
+    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version
+
+- **Event Stream**
+  - Why it exists: tuning and incident response without coupling telemetry to request latency.
+  - Properties:
+    - edge has a bounded queue; events drop when full
+    - allows are sampled; blocks/challenges are prioritized
+    - local counters/metrics always emitted even if detailed events drop
+
+## Deep Dive: Line-Speed Detection Without Regex DoS
+
+### 1) Canonicalization first, with a clear contract
+The data plane normalizes inputs so matching is predictable:
+- Percent-decoding with recursion limits and strict invalid-encoding rejection
+- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
+- Header/cookie parsing with bounded field counts and strict duplicate-header policy
+- UTF-8 validation; invalid encodings are immediate block signals
+
+Shadow mode is used to introduce stricter normalization safely (observe before enforce).
+
+### 2) Compile rules into bounded-time primitives
+- Multi-pattern scan for fixed substrings (Aho–Corasick class)
+- Safe regex only (RE2-class; capped by inspected length and match work)
+- Small scoring only where needed (weak signals add up; strong signals act alone)
+
+### 3) Streaming body inspection with hard caps
+- Only inspect bodies on routes that accept user input, up to a route byte budget.
+- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
+- Stop inspecting after the budget and continue forwarding the stream.
+
+### 4) Bounded execution enforcement and safe rollouts
+Every request has a fixed work budget:
+- per-phase byte limits (decoded URL/headers/cookies/body)
+- per-rule match work caps and a global per-request time budget
+- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work
+
+Every rollout is gated:
+- artifacts are immutable, signed, and verified in the POP before activation
+- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
+- rollback is a version pin; POPs revert on next poll without needing new artifacts
+
+## Trade-offs
+
+| Optimized For | Sacrificed |
+|--------------|------------|
+| Predictable latency under attack | Deep inspection of huge/streaming bodies |
+| Offline-safe edge operation | “Live” per-request policy decisions |
+| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
+| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |
+
+## Failure Modes
+
+- **Bad ruleset blocks legitimate traffic**
+  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
+  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.
+
+- **Control plane down for minutes**
+  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
+  - Recover: restore control plane; no edge impact required.
+
+- **Network partition between POPs and control plane**
+  - What happens: POPs keep pinned version; upgrades freeze.
+  - Recover: connectivity returns; rollouts resume from known version state.
+
+- **Event stream slow or down**
+  - What happens: edge drops events once queues fill; request latency unaffected.
+  - Recover: telemetry returns; counters show the gap; no edge change required.
+
+- **CPU saturation at POP during attack**
+  - Detect: rising evaluation time, queue depth, p99.
+  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.
+
+## What We Removed
+
+- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
+- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
+- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
+- Unbounded matching constructs: only bounded primitives and safe regex.
+
+## Operational Notes
+
+- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
+- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
+- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
+```

exec
/bin/zsh -lc 'rm -f .tmp-waf.mmd .tmp-waf.svg' in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 49ms:


thinking
**Checking markdown validity**

I’m facing an issue with validation failing due to Puppeteer. So, I need to perform a manual sanity check. I’ll read the file snippet to make sure the markdown fences are correct. To do this efficiently, I'll use `sed -n`. It’s a straightforward approach, and I’ll keep an eye out for any potential errors in my process while ensuring everything checks out properly! Let's see how this goes!
file update:
diff --git a/solutions-v2/08-security-access-control/web-application-firewall.md b/solutions-v2/08-security-access-control/web-application-firewall.md
new file mode 100644
index 0000000000000000000000000000000000000000..82f500e2eb9cd7377a941bf0ec5280c3177cea62
--- /dev/null
+++ b/solutions-v2/08-security-access-control/web-application-firewall.md
@@ -0,0 +1,174 @@
+```markdown
+---
+title: "Web Application Firewall (WAF)"
+category: "Security & Access Control"
+difficulty: "Hard"
+tags: [security, edge, waf, http, ddos, rules, observability]
+---
+
+## Overview
+
+This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.
+
+The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.
+
+## What Makes This Hard
+
+1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
+2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
+3. **Global blast radius:** one bad update can break every property faster than any application deploy.
+
+## Requirements
+
+### Functional Requirements
+- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
+- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
+- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
+- Provide shadow mode and explainability: stable rule IDs + matched locations.
+- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
+- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
+- Ensure observability cannot add latency: bounded queues + drop-on-overload.
+
+### Scale Targets
+- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
+- **Latency budget:** +2 ms p99 added by WAF at the edge.
+- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
+- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.
+
+## Key Design Decisions
+
+- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
+  - Rejected: custom proxy stack or per-request scripting
+  - Why: keep protocol handling boring and keep evaluation bounded.
+
+- **Choose: signed, versioned rule artifacts pulled by POPs**
+  - Rejected: push-based “distributor” dependencies in the hot path
+  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.
+
+- **Choose: bounded parsing and matching with runtime circuit breakers**
+  - Rejected: “full inspection” by buffering and best-effort timeouts
+  - Why: adversaries control input; the WAF must control worst-case work.
+
+- **Choose: stateless challenges**
+  - Rejected: global state stores or per-request lookups for challenges
+  - Why: challenges must not introduce a new availability dependency.
+
+## Architecture
+
+```mermaid
+flowchart LR
+  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
+  E --> O[Origin App]
+  E --> L[Event Stream]
+  CP[Rule Control Plane] --> E
+  L --> CP
+```
+
+### Components
+
+- **Edge Proxy**
+  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
+  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.
+
+- **WAF Filter (Data Plane)**
+  - Why it exists: the only place decisions must be made per request with a strict latency budget.
+  - Properties:
+    - deterministic evaluation (no network calls)
+    - strict caps (decoded bytes, token counts, match work)
+    - verifies rule artifact signatures and hot-swaps by version
+    - challenges are stateless (signed short-TTL token cookie)
+
+- **Origin App**
+  - Why it exists: the system being protected; WAF is additive.
+
+- **Rule Control Plane**
+  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
+  - Responsibilities:
+    - compile rules into bounded match primitives and reject unsafe constructs
+    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
+    - sign and publish immutable rule artifacts by version
+    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version
+
+- **Event Stream**
+  - Why it exists: tuning and incident response without coupling telemetry to request latency.
+  - Properties:
+    - edge has a bounded queue; events drop when full
+    - allows are sampled; blocks/challenges are prioritized
+    - local counters/metrics always emitted even if detailed events drop
+
+## Deep Dive: Line-Speed Detection Without Regex DoS
+
+### 1) Canonicalization first, with a clear contract
+The data plane normalizes inputs so matching is predictable:
+- Percent-decoding with recursion limits and strict invalid-encoding rejection
+- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
+- Header/cookie parsing with bounded field counts and strict duplicate-header policy
+- UTF-8 validation; invalid encodings are immediate block signals
+
+Shadow mode is used to introduce stricter normalization safely (observe before enforce).
+
+### 2) Compile rules into bounded-time primitives
+- Multi-pattern scan for fixed substrings (Aho–Corasick class)
+- Safe regex only (RE2-class; capped by inspected length and match work)
+- Small scoring only where needed (weak signals add up; strong signals act alone)
+
+### 3) Streaming body inspection with hard caps
+- Only inspect bodies on routes that accept user input, up to a route byte budget.
+- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
+- Stop inspecting after the budget and continue forwarding the stream.
+
+### 4) Bounded execution enforcement and safe rollouts
+Every request has a fixed work budget:
+- per-phase byte limits (decoded URL/headers/cookies/body)
+- per-rule match work caps and a global per-request time budget
+- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work
+
+Every rollout is gated:
+- artifacts are immutable, signed, and verified in the POP before activation
+- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
+- rollback is a version pin; POPs revert on next poll without needing new artifacts
+
+## Trade-offs
+
+| Optimized For | Sacrificed |
+|--------------|------------|
+| Predictable latency under attack | Deep inspection of huge/streaming bodies |
+| Offline-safe edge operation | “Live” per-request policy decisions |
+| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
+| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |
+
+## Failure Modes
+
+- **Bad ruleset blocks legitimate traffic**
+  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
+  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.
+
+- **Control plane down for minutes**
+  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
+  - Recover: restore control plane; no edge impact required.
+
+- **Network partition between POPs and control plane**
+  - What happens: POPs keep pinned version; upgrades freeze.
+  - Recover: connectivity returns; rollouts resume from known version state.
+
+- **Event stream slow or down**
+  - What happens: edge drops events once queues fill; request latency unaffected.
+  - Recover: telemetry returns; counters show the gap; no edge change required.
+
+- **CPU saturation at POP during attack**
+  - Detect: rising evaluation time, queue depth, p99.
+  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.
+
+## What We Removed
+
+- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
+- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
+- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
+- Unbounded matching constructs: only bounded primitives and safe regex.
+
+## Operational Notes
+
+- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
+- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
+- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
+```

exec
/bin/zsh -lc "sed -n '1,220p' solutions-v2/08-security-access-control/web-application-firewall.md" in /Users/dominikdabrowski/pr/system-design-atlas succeeded in 47ms:
```markdown
---
title: "Web Application Firewall (WAF)"
category: "Security & Access Control"
difficulty: "Hard"
tags: [security, edge, waf, http, ddos, rules, observability]
---

## Overview

This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.

The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.

## What Makes This Hard

1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
3. **Global blast radius:** one bad update can break every property faster than any application deploy.





thinking
**Refining security notes**

Alright, I want to include some important details about canary gates, like mentioning edge CPU/time budgets and runtime budgets. I should also address the challenge path for users, particularly failure behavior if token verification fails or due to clock skew. It seems I could add to the operational notes about challenge fallback if the token fails, treating it as no token and showing the challenge again.  

Additionally, I need to mention the key rotation plan clearly, including that both keys are accepted during their overlap period for TTL. I’ll also consider artifact key rotation and make sure to add that root key is pinned in binary.
thinking
**Updating architecture diagrams**

I want to maintain a minimal approach, but I need to address the “control plane down for 5 min” issue. The architecture diagram has an arrow from CP to E, implying a push, while we've described it as a pull. I could consider relabeling it as "poll," but that may not be necessary. Perhaps I’ll update the diagram to show it as `E --poll--> CP`.  

But ultimately, I’ll keep it straightforward: CP will publish artifacts to E, and telemetry can flow from E to L. This way, I don't add unnecessary components, and the diagram should work well.
thinking
**Ensuring clarity in architecture**

I'll show the pull mechanism in the diagrams with `E -. polls .-> CP` and `CP -. artifacts .-> E`, or I could simply adjust the arrow from `CP -> E` to `CP -->|signed artifacts| E` and specify in the text that POP pulls. It’s important to make sure that POPs only advance when allowed by the rollout state, yet I must consider that if the control plane is down, POP will maintain its pinned version, which is acceptable.  

To simplify matters, I might focus on the artifact version so that the POP polling returns the current version file, without adding new components. I’ll also ensure that “per-route policy” remains intact without needing per-request network calls. Lastly, I need to update plan step 4 to reflect that sanity checks are in progress.
thinking
**Updating progress steps**

I think it's time to mark our progress now, as it's pending. I might consider running `rg` or perhaps check `scripts/validate-mermaid.sh`, but that doesn't seem necessary at this point. Instead, I could look into `solutions-v2/08-security-access-control` to ensure consistency with file names and possibly update an index, although that likely isn't needed. 

So, I’ll just mark step 4 as complete, while keeping step 3 in progress, and then set both to completed as I update the plan accordingly.
file update:
diff --git a/solutions-v2/08-security-access-control/web-application-firewall.md b/solutions-v2/08-security-access-control/web-application-firewall.md
new file mode 100644
index 0000000000000000000000000000000000000000..82f500e2eb9cd7377a941bf0ec5280c3177cea62
--- /dev/null
+++ b/solutions-v2/08-security-access-control/web-application-firewall.md
@@ -0,0 +1,174 @@
+```markdown
+---
+title: "Web Application Firewall (WAF)"
+category: "Security & Access Control"
+difficulty: "Hard"
+tags: [security, edge, waf, http, ddos, rules, observability]
+---
+
+## Overview
+
+This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.
+
+The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.
+
+## What Makes This Hard
+
+1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
+2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
+3. **Global blast radius:** one bad update can break every property faster than any application deploy.
+
+## Requirements
+
+### Functional Requirements
+- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
+- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
+- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
+- Provide shadow mode and explainability: stable rule IDs + matched locations.
+- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
+- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
+- Ensure observability cannot add latency: bounded queues + drop-on-overload.
+
+### Scale Targets
+- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
+- **Latency budget:** +2 ms p99 added by WAF at the edge.
+- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
+- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.
+
+## Key Design Decisions
+
+- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
+  - Rejected: custom proxy stack or per-request scripting
+  - Why: keep protocol handling boring and keep evaluation bounded.
+
+- **Choose: signed, versioned rule artifacts pulled by POPs**
+  - Rejected: push-based “distributor” dependencies in the hot path
+  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.
+
+- **Choose: bounded parsing and matching with runtime circuit breakers**
+  - Rejected: “full inspection” by buffering and best-effort timeouts
+  - Why: adversaries control input; the WAF must control worst-case work.
+
+- **Choose: stateless challenges**
+  - Rejected: global state stores or per-request lookups for challenges
+  - Why: challenges must not introduce a new availability dependency.
+
+## Architecture
+
+```mermaid
+flowchart LR
+  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
+  E --> O[Origin App]
+  E --> L[Event Stream]
+  CP[Rule Control Plane] --> E
+  L --> CP
+```
+
+### Components
+
+- **Edge Proxy**
+  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
+  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.
+
+- **WAF Filter (Data Plane)**
+  - Why it exists: the only place decisions must be made per request with a strict latency budget.
+  - Properties:
+    - deterministic evaluation (no network calls)
+    - strict caps (decoded bytes, token counts, match work)
+    - verifies rule artifact signatures and hot-swaps by version
+    - challenges are stateless (signed short-TTL token cookie)
+
+- **Origin App**
+  - Why it exists: the system being protected; WAF is additive.
+
+- **Rule Control Plane**
+  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
+  - Responsibilities:
+    - compile rules into bounded match primitives and reject unsafe constructs
+    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
+    - sign and publish immutable rule artifacts by version
+    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version
+
+- **Event Stream**
+  - Why it exists: tuning and incident response without coupling telemetry to request latency.
+  - Properties:
+    - edge has a bounded queue; events drop when full
+    - allows are sampled; blocks/challenges are prioritized
+    - local counters/metrics always emitted even if detailed events drop
+
+## Deep Dive: Line-Speed Detection Without Regex DoS
+
+### 1) Canonicalization first, with a clear contract
+The data plane normalizes inputs so matching is predictable:
+- Percent-decoding with recursion limits and strict invalid-encoding rejection
+- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
+- Header/cookie parsing with bounded field counts and strict duplicate-header policy
+- UTF-8 validation; invalid encodings are immediate block signals
+
+Shadow mode is used to introduce stricter normalization safely (observe before enforce).
+
+### 2) Compile rules into bounded-time primitives
+- Multi-pattern scan for fixed substrings (Aho–Corasick class)
+- Safe regex only (RE2-class; capped by inspected length and match work)
+- Small scoring only where needed (weak signals add up; strong signals act alone)
+
+### 3) Streaming body inspection with hard caps
+- Only inspect bodies on routes that accept user input, up to a route byte budget.
+- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
+- Stop inspecting after the budget and continue forwarding the stream.
+
+### 4) Bounded execution enforcement and safe rollouts
+Every request has a fixed work budget:
+- per-phase byte limits (decoded URL/headers/cookies/body)
+- per-rule match work caps and a global per-request time budget
+- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work
+
+Every rollout is gated:
+- artifacts are immutable, signed, and verified in the POP before activation
+- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
+- rollback is a version pin; POPs revert on next poll without needing new artifacts
+
+## Trade-offs
+
+| Optimized For | Sacrificed |
+|--------------|------------|
+| Predictable latency under attack | Deep inspection of huge/streaming bodies |
+| Offline-safe edge operation | “Live” per-request policy decisions |
+| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
+| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |
+
+## Failure Modes
+
+- **Bad ruleset blocks legitimate traffic**
+  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
+  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.
+
+- **Control plane down for minutes**
+  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
+  - Recover: restore control plane; no edge impact required.
+
+- **Network partition between POPs and control plane**
+  - What happens: POPs keep pinned version; upgrades freeze.
+  - Recover: connectivity returns; rollouts resume from known version state.
+
+- **Event stream slow or down**
+  - What happens: edge drops events once queues fill; request latency unaffected.
+  - Recover: telemetry returns; counters show the gap; no edge change required.
+
+- **CPU saturation at POP during attack**
+  - Detect: rising evaluation time, queue depth, p99.
+  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.
+
+## What We Removed
+
+- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
+- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
+- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
+- Unbounded matching constructs: only bounded primitives and safe regex.
+
+## Operational Notes
+
+- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
+- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
+- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
+```

Plan update
  ✓ Locate existing WAF markdown file
  ✓ Draft simplified design content
  ✓ Update file and diagrams
  → Sanity-check markdown rendering

thinking
**Finalizing checks**

I need to do a final sanity check on the fence balancing. Everything seems to be okay, but I might as well ensure there are no trailing spaces, even if it's not essential. I'll also check that the file ends with those backticks ` ``` `. It does! I'll just confirm if the `.tmp` files are removed, and once that's all clear, I can complete the final planning step.
file update:
diff --git a/solutions-v2/08-security-access-control/web-application-firewall.md b/solutions-v2/08-security-access-control/web-application-firewall.md
new file mode 100644
index 0000000000000000000000000000000000000000..82f500e2eb9cd7377a941bf0ec5280c3177cea62
--- /dev/null
+++ b/solutions-v2/08-security-access-control/web-application-firewall.md
@@ -0,0 +1,174 @@
+```markdown
+---
+title: "Web Application Firewall (WAF)"
+category: "Security & Access Control"
+difficulty: "Hard"
+tags: [security, edge, waf, http, ddos, rules, observability]
+---
+
+## Overview
+
+This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.
+
+The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.
+
+## What Makes This Hard
+
+1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
+2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
+3. **Global blast radius:** one bad update can break every property faster than any application deploy.
+
+## Requirements
+
+### Functional Requirements
+- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
+- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
+- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
+- Provide shadow mode and explainability: stable rule IDs + matched locations.
+- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
+- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
+- Ensure observability cannot add latency: bounded queues + drop-on-overload.
+
+### Scale Targets
+- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
+- **Latency budget:** +2 ms p99 added by WAF at the edge.
+- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
+- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.
+
+## Key Design Decisions
+
+- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
+  - Rejected: custom proxy stack or per-request scripting
+  - Why: keep protocol handling boring and keep evaluation bounded.
+
+- **Choose: signed, versioned rule artifacts pulled by POPs**
+  - Rejected: push-based “distributor” dependencies in the hot path
+  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.
+
+- **Choose: bounded parsing and matching with runtime circuit breakers**
+  - Rejected: “full inspection” by buffering and best-effort timeouts
+  - Why: adversaries control input; the WAF must control worst-case work.
+
+- **Choose: stateless challenges**
+  - Rejected: global state stores or per-request lookups for challenges
+  - Why: challenges must not introduce a new availability dependency.
+
+## Architecture
+
+```mermaid
+flowchart LR
+  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
+  E --> O[Origin App]
+  E --> L[Event Stream]
+  CP[Rule Control Plane] --> E
+  L --> CP
+```
+
+### Components
+
+- **Edge Proxy**
+  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
+  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.
+
+- **WAF Filter (Data Plane)**
+  - Why it exists: the only place decisions must be made per request with a strict latency budget.
+  - Properties:
+    - deterministic evaluation (no network calls)
+    - strict caps (decoded bytes, token counts, match work)
+    - verifies rule artifact signatures and hot-swaps by version
+    - challenges are stateless (signed short-TTL token cookie)
+
+- **Origin App**
+  - Why it exists: the system being protected; WAF is additive.
+
+- **Rule Control Plane**
+  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
+  - Responsibilities:
+    - compile rules into bounded match primitives and reject unsafe constructs
+    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
+    - sign and publish immutable rule artifacts by version
+    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version
+
+- **Event Stream**
+  - Why it exists: tuning and incident response without coupling telemetry to request latency.
+  - Properties:
+    - edge has a bounded queue; events drop when full
+    - allows are sampled; blocks/challenges are prioritized
+    - local counters/metrics always emitted even if detailed events drop
+
+## Deep Dive: Line-Speed Detection Without Regex DoS
+
+### 1) Canonicalization first, with a clear contract
+The data plane normalizes inputs so matching is predictable:
+- Percent-decoding with recursion limits and strict invalid-encoding rejection
+- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
+- Header/cookie parsing with bounded field counts and strict duplicate-header policy
+- UTF-8 validation; invalid encodings are immediate block signals
+
+Shadow mode is used to introduce stricter normalization safely (observe before enforce).
+
+### 2) Compile rules into bounded-time primitives
+- Multi-pattern scan for fixed substrings (Aho–Corasick class)
+- Safe regex only (RE2-class; capped by inspected length and match work)
+- Small scoring only where needed (weak signals add up; strong signals act alone)
+
+### 3) Streaming body inspection with hard caps
+- Only inspect bodies on routes that accept user input, up to a route byte budget.
+- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
+- Stop inspecting after the budget and continue forwarding the stream.
+
+### 4) Bounded execution enforcement and safe rollouts
+Every request has a fixed work budget:
+- per-phase byte limits (decoded URL/headers/cookies/body)
+- per-rule match work caps and a global per-request time budget
+- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work
+
+Every rollout is gated:
+- artifacts are immutable, signed, and verified in the POP before activation
+- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
+- rollback is a version pin; POPs revert on next poll without needing new artifacts
+
+## Trade-offs
+
+| Optimized For | Sacrificed |
+|--------------|------------|
+| Predictable latency under attack | Deep inspection of huge/streaming bodies |
+| Offline-safe edge operation | “Live” per-request policy decisions |
+| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
+| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |
+
+## Failure Modes
+
+- **Bad ruleset blocks legitimate traffic**
+  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
+  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.
+
+- **Control plane down for minutes**
+  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
+  - Recover: restore control plane; no edge impact required.
+
+- **Network partition between POPs and control plane**
+  - What happens: POPs keep pinned version; upgrades freeze.
+  - Recover: connectivity returns; rollouts resume from known version state.
+
+- **Event stream slow or down**
+  - What happens: edge drops events once queues fill; request latency unaffected.
+  - Recover: telemetry returns; counters show the gap; no edge change required.
+
+- **CPU saturation at POP during attack**
+  - Detect: rising evaluation time, queue depth, p99.
+  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.
+
+## What We Removed
+
+- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
+- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
+- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
+- Unbounded matching constructs: only bounded primitives and safe regex.
+
+## Operational Notes
+
+- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
+- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
+- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
+```

Plan update
  ✓ Locate existing WAF markdown file
  ✓ Draft simplified design content
  ✓ Update file and diagrams
  ✓ Sanity-check markdown rendering



thinking
**Considering output format**

Hmm, the repo file has an extra wrapper, so I’m debating whether to remove it to align with the expected output. I see that the solutions-v2 entries keep the wrapper, which makes me think mine should too. But if I output in a code block with the wrapper, it could cause nested fences to break. I’ll adjust to use an outer text fence instead, which would allow including inner fences. However, since the user requested the complete markdown document, I think I can deliver it as plain text without the added wrapper.
thinking
**Deciding on output format**

I’m planning to output the response as raw markdown instead of in a code block. While this could lead to the frontmatter appearing as horizontal lines, I think it might be alright. But using a code block seems safer, so I’ll go with it and ensure there's no internal fencing. I'll copy the content while removing the first and last lines, and I should mention the updated file path, as it could be helpful. Although the user didn't ask for it, including it might be beneficial. My output will ensure all necessary sections are included as requested.
thinking
**Finalizing component simplification**

I've simplified the project down to five components, which feels like a good move! It makes it more manageable and clear. Now I think it's time to finalize everything. I'll make sure that all the details align with this simplification and present it effectively. This ensures that the information is concise and understandable, helping users grasp the key points without any unnecessary complexity. Alright, let's wrap this up!
codex
Saved as `solutions-v2/08-security-access-control/web-application-firewall.md`.

```markdown
---
title: "Web Application Firewall (WAF)"
category: "Security & Access Control"
difficulty: "Hard"
tags: [security, edge, waf, http, ddos, rules, observability]
---

## Overview

This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.

The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.

## What Makes This Hard

1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
3. **Global blast radius:** one bad update can break every property faster than any application deploy.

## Requirements

### Functional Requirements
- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
- Provide shadow mode and explainability: stable rule IDs + matched locations.
- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
- Ensure observability cannot add latency: bounded queues + drop-on-overload.

### Scale Targets
- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
- **Latency budget:** +2 ms p99 added by WAF at the edge.
- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.

## Key Design Decisions

- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
  - Rejected: custom proxy stack or per-request scripting
  - Why: keep protocol handling boring and keep evaluation bounded.

- **Choose: signed, versioned rule artifacts pulled by POPs**
  - Rejected: push-based “distributor” dependencies in the hot path
  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.

- **Choose: bounded parsing and matching with runtime circuit breakers**
  - Rejected: “full inspection” by buffering and best-effort timeouts
  - Why: adversaries control input; the WAF must control worst-case work.

- **Choose: stateless challenges**
  - Rejected: global state stores or per-request lookups for challenges
  - Why: challenges must not introduce a new availability dependency.

## Architecture

```mermaid
flowchart LR
  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
  E --> O[Origin App]
  E --> L[Event Stream]
  CP[Rule Control Plane] --> E
  L --> CP
```

### Components

- **Edge Proxy**
  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.

- **WAF Filter (Data Plane)**
  - Why it exists: the only place decisions must be made per request with a strict latency budget.
  - Properties:
    - deterministic evaluation (no network calls)
    - strict caps (decoded bytes, token counts, match work)
    - verifies rule artifact signatures and hot-swaps by version
    - challenges are stateless (signed short-TTL token cookie)

- **Origin App**
  - Why it exists: the system being protected; WAF is additive.

- **Rule Control Plane**
  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
  - Responsibilities:
    - compile rules into bounded match primitives and reject unsafe constructs
    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
    - sign and publish immutable rule artifacts by version
    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version

- **Event Stream**
  - Why it exists: tuning and incident response without coupling telemetry to request latency.
  - Properties:
    - edge has a bounded queue; events drop when full
    - allows are sampled; blocks/challenges are prioritized
    - local counters/metrics always emitted even if detailed events drop

## Deep Dive: Line-Speed Detection Without Regex DoS

### 1) Canonicalization first, with a clear contract
The data plane normalizes inputs so matching is predictable:
- Percent-decoding with recursion limits and strict invalid-encoding rejection
- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
- Header/cookie parsing with bounded field counts and strict duplicate-header policy
- UTF-8 validation; invalid encodings are immediate block signals

Shadow mode is used to introduce stricter normalization safely (observe before enforce).

### 2) Compile rules into bounded-time primitives
- Multi-pattern scan for fixed substrings (Aho–Corasick class)
- Safe regex only (RE2-class; capped by inspected length and match work)
- Small scoring only where needed (weak signals add up; strong signals act alone)

### 3) Streaming body inspection with hard caps
- Only inspect bodies on routes that accept user input, up to a route byte budget.
- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
- Stop inspecting after the budget and continue forwarding the stream.

### 4) Bounded execution enforcement and safe rollouts
Every request has a fixed work budget:
- per-phase byte limits (decoded URL/headers/cookies/body)
- per-rule match work caps and a global per-request time budget
- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work

Every rollout is gated:
- artifacts are immutable, signed, and verified in the POP before activation
- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
- rollback is a version pin; POPs revert on next poll without needing new artifacts

## Trade-offs

| Optimized For | Sacrificed |
|--------------|------------|
| Predictable latency under attack | Deep inspection of huge/streaming bodies |
| Offline-safe edge operation | “Live” per-request policy decisions |
| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |

## Failure Modes

- **Bad ruleset blocks legitimate traffic**
  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.

- **Control plane down for minutes**
  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
  - Recover: restore control plane; no edge impact required.

- **Network partition between POPs and control plane**
  - What happens: POPs keep pinned version; upgrades freeze.
  - Recover: connectivity returns; rollouts resume from known version state.

- **Event stream slow or down**
  - What happens: edge drops events once queues fill; request latency unaffected.
  - Recover: telemetry returns; counters show the gap; no edge change required.

- **CPU saturation at POP during attack**
  - Detect: rising evaluation time, queue depth, p99.
  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.

## What We Removed

- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
- Unbounded matching constructs: only bounded primitives and safe regex.

## Operational Notes

- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
```
file update:
diff --git a/solutions-v2/08-security-access-control/web-application-firewall.md b/solutions-v2/08-security-access-control/web-application-firewall.md
new file mode 100644
index 0000000000000000000000000000000000000000..82f500e2eb9cd7377a941bf0ec5280c3177cea62
--- /dev/null
+++ b/solutions-v2/08-security-access-control/web-application-firewall.md
@@ -0,0 +1,174 @@
+```markdown
+---
+title: "Web Application Firewall (WAF)"
+category: "Security & Access Control"
+difficulty: "Hard"
+tags: [security, edge, waf, http, ddos, rules, observability]
+---
+
+## Overview
+
+This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.
+
+The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.
+
+## What Makes This Hard
+
+1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
+2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
+3. **Global blast radius:** one bad update can break every property faster than any application deploy.
+
+## Requirements
+
+### Functional Requirements
+- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
+- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
+- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
+- Provide shadow mode and explainability: stable rule IDs + matched locations.
+- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
+- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
+- Ensure observability cannot add latency: bounded queues + drop-on-overload.
+
+### Scale Targets
+- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
+- **Latency budget:** +2 ms p99 added by WAF at the edge.
+- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
+- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.
+
+## Key Design Decisions
+
+- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
+  - Rejected: custom proxy stack or per-request scripting
+  - Why: keep protocol handling boring and keep evaluation bounded.
+
+- **Choose: signed, versioned rule artifacts pulled by POPs**
+  - Rejected: push-based “distributor” dependencies in the hot path
+  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.
+
+- **Choose: bounded parsing and matching with runtime circuit breakers**
+  - Rejected: “full inspection” by buffering and best-effort timeouts
+  - Why: adversaries control input; the WAF must control worst-case work.
+
+- **Choose: stateless challenges**
+  - Rejected: global state stores or per-request lookups for challenges
+  - Why: challenges must not introduce a new availability dependency.
+
+## Architecture
+
+```mermaid
+flowchart LR
+  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
+  E --> O[Origin App]
+  E --> L[Event Stream]
+  CP[Rule Control Plane] --> E
+  L --> CP
+```
+
+### Components
+
+- **Edge Proxy**
+  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
+  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.
+
+- **WAF Filter (Data Plane)**
+  - Why it exists: the only place decisions must be made per request with a strict latency budget.
+  - Properties:
+    - deterministic evaluation (no network calls)
+    - strict caps (decoded bytes, token counts, match work)
+    - verifies rule artifact signatures and hot-swaps by version
+    - challenges are stateless (signed short-TTL token cookie)
+
+- **Origin App**
+  - Why it exists: the system being protected; WAF is additive.
+
+- **Rule Control Plane**
+  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
+  - Responsibilities:
+    - compile rules into bounded match primitives and reject unsafe constructs
+    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
+    - sign and publish immutable rule artifacts by version
+    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version
+
+- **Event Stream**
+  - Why it exists: tuning and incident response without coupling telemetry to request latency.
+  - Properties:
+    - edge has a bounded queue; events drop when full
+    - allows are sampled; blocks/challenges are prioritized
+    - local counters/metrics always emitted even if detailed events drop
+
+## Deep Dive: Line-Speed Detection Without Regex DoS
+
+### 1) Canonicalization first, with a clear contract
+The data plane normalizes inputs so matching is predictable:
+- Percent-decoding with recursion limits and strict invalid-encoding rejection
+- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
+- Header/cookie parsing with bounded field counts and strict duplicate-header policy
+- UTF-8 validation; invalid encodings are immediate block signals
+
+Shadow mode is used to introduce stricter normalization safely (observe before enforce).
+
+### 2) Compile rules into bounded-time primitives
+- Multi-pattern scan for fixed substrings (Aho–Corasick class)
+- Safe regex only (RE2-class; capped by inspected length and match work)
+- Small scoring only where needed (weak signals add up; strong signals act alone)
+
+### 3) Streaming body inspection with hard caps
+- Only inspect bodies on routes that accept user input, up to a route byte budget.
+- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
+- Stop inspecting after the budget and continue forwarding the stream.
+
+### 4) Bounded execution enforcement and safe rollouts
+Every request has a fixed work budget:
+- per-phase byte limits (decoded URL/headers/cookies/body)
+- per-rule match work caps and a global per-request time budget
+- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work
+
+Every rollout is gated:
+- artifacts are immutable, signed, and verified in the POP before activation
+- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
+- rollback is a version pin; POPs revert on next poll without needing new artifacts
+
+## Trade-offs
+
+| Optimized For | Sacrificed |
+|--------------|------------|
+| Predictable latency under attack | Deep inspection of huge/streaming bodies |
+| Offline-safe edge operation | “Live” per-request policy decisions |
+| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
+| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |
+
+## Failure Modes
+
+- **Bad ruleset blocks legitimate traffic**
+  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
+  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.
+
+- **Control plane down for minutes**
+  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
+  - Recover: restore control plane; no edge impact required.
+
+- **Network partition between POPs and control plane**
+  - What happens: POPs keep pinned version; upgrades freeze.
+  - Recover: connectivity returns; rollouts resume from known version state.
+
+- **Event stream slow or down**
+  - What happens: edge drops events once queues fill; request latency unaffected.
+  - Recover: telemetry returns; counters show the gap; no edge change required.
+
+- **CPU saturation at POP during attack**
+  - Detect: rising evaluation time, queue depth, p99.
+  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.
+
+## What We Removed
+
+- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
+- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
+- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
+- Unbounded matching constructs: only bounded primitives and safe regex.
+
+## Operational Notes
+
+- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
+- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
+- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
+```

tokens used
82,453
Saved as `solutions-v2/08-security-access-control/web-application-firewall.md`.

```markdown
---
title: "Web Application Firewall (WAF)"
category: "Security & Access Control"
difficulty: "Hard"
tags: [security, edge, waf, http, ddos, rules, observability]
---

## Overview

This system is an **edge WAF** that sits in front of web applications and blocks SQL injection, XSS, and malicious payloads **at line speed** without becoming the bottleneck.

The WAF is a **deterministic, precompiled filter** that runs inside a mature edge proxy (no dynamic policy code in the request path). Everything else—rule authoring, validation, safe rollout/rollback, and telemetry—lives in a small control plane.

## What Makes This Hard

1. **Adversarial cost:** attackers try to turn parsing and matching into CPU/memory bombs (giant headers, chunked bodies, encoding tricks, regex abuse).
2. **False positives:** blocking real users is worse than missing a niche signature; rules must ship with safety gates.
3. **Global blast radius:** one bad update can break every property faster than any application deploy.

## Requirements

### Functional Requirements
- Detect and mitigate common injection classes (SQLi, XSS, command injection, path traversal) across URL, headers, cookies, query params, and common body formats.
- Support actions: `ALLOW`, `BLOCK`, `CHALLENGE`, `LOG_ONLY`.
- Support per-route policy (e.g., stricter on `/login`) without per-request network calls.
- Provide shadow mode and explainability: stable rule IDs + matched locations.
- Provide safe rollout: shadow → canary POPs → wider rollout, plus instant rollback.
- Continue serving safely on control-plane outage: POPs run on last-known-good rules indefinitely.
- Ensure observability cannot add latency: bounded queues + drop-on-overload.

### Scale Targets
- **Traffic:** 1M requests/sec aggregate; 50k RPS burst per POP.
- **Latency budget:** +2 ms p99 added by WAF at the edge.
- **Payload limits:** up to 32 KB headers, 1 MB body (route-configurable hard caps).
- **Ruleset size:** 10k–50k signatures + heuristics compiled to bounded primitives.

## Key Design Decisions

- **Choose: WAF as an Envoy/Nginx filter with precompiled rules**
  - Rejected: custom proxy stack or per-request scripting
  - Why: keep protocol handling boring and keep evaluation bounded.

- **Choose: signed, versioned rule artifacts pulled by POPs**
  - Rejected: push-based “distributor” dependencies in the hot path
  - Why: POPs keep last-known-good, upgrades are explicit, rollback is instant.

- **Choose: bounded parsing and matching with runtime circuit breakers**
  - Rejected: “full inspection” by buffering and best-effort timeouts
  - Why: adversaries control input; the WAF must control worst-case work.

- **Choose: stateless challenges**
  - Rejected: global state stores or per-request lookups for challenges
  - Why: challenges must not introduce a new availability dependency.

## Architecture

```mermaid
flowchart LR
  C[Client] --> E[Edge Proxy<br/>+ WAF Filter]
  E --> O[Origin App]
  E --> L[Event Stream]
  CP[Rule Control Plane] --> E
  L --> CP
```

### Components

- **Edge Proxy**
  - Why it exists: TLS termination, strict HTTP parsing, and hard caps are the cheapest place to stop junk traffic.
  - Simplified: use a mature proxy (Envoy/Nginx) for HTTP/2/3 edge cases and smuggling defenses.

- **WAF Filter (Data Plane)**
  - Why it exists: the only place decisions must be made per request with a strict latency budget.
  - Properties:
    - deterministic evaluation (no network calls)
    - strict caps (decoded bytes, token counts, match work)
    - verifies rule artifact signatures and hot-swaps by version
    - challenges are stateless (signed short-TTL token cookie)

- **Origin App**
  - Why it exists: the system being protected; WAF is additive.

- **Rule Control Plane**
  - Why it exists: rules are code; they need validation, provenance, staged rollout, and rollback.
  - Responsibilities:
    - compile rules into bounded match primitives and reject unsafe constructs
    - run pre-deploy cost gates (worst-case corpus microbench + size caps)
    - sign and publish immutable rule artifacts by version
    - orchestrate rollout state (shadow/canary/global) and expose rollback by pinning a prior version

- **Event Stream**
  - Why it exists: tuning and incident response without coupling telemetry to request latency.
  - Properties:
    - edge has a bounded queue; events drop when full
    - allows are sampled; blocks/challenges are prioritized
    - local counters/metrics always emitted even if detailed events drop

## Deep Dive: Line-Speed Detection Without Regex DoS

### 1) Canonicalization first, with a clear contract
The data plane normalizes inputs so matching is predictable:
- Percent-decoding with recursion limits and strict invalid-encoding rejection
- Path normalization (`/a/../b` → `/b`) with strict dot-segment handling
- Header/cookie parsing with bounded field counts and strict duplicate-header policy
- UTF-8 validation; invalid encodings are immediate block signals

Shadow mode is used to introduce stricter normalization safely (observe before enforce).

### 2) Compile rules into bounded-time primitives
- Multi-pattern scan for fixed substrings (Aho–Corasick class)
- Safe regex only (RE2-class; capped by inspected length and match work)
- Small scoring only where needed (weak signals add up; strong signals act alone)

### 3) Streaming body inspection with hard caps
- Only inspect bodies on routes that accept user input, up to a route byte budget.
- Parse incrementally for `application/x-www-form-urlencoded` and `application/json`; scan decoded values.
- Stop inspecting after the budget and continue forwarding the stream.

### 4) Bounded execution enforcement and safe rollouts
Every request has a fixed work budget:
- per-phase byte limits (decoded URL/headers/cookies/body)
- per-rule match work caps and a global per-request time budget
- circuit breaker: when budgets are exceeded, skip remaining expensive phases (e.g., body inspection) and prefer `CHALLENGE`/`LOG_ONLY` over unpredictable work

Every rollout is gated:
- artifacts are immutable, signed, and verified in the POP before activation
- POPs only advance to new versions when explicitly allowed by rollout state; otherwise they keep the pinned version
- rollback is a version pin; POPs revert on next poll without needing new artifacts

## Trade-offs

| Optimized For | Sacrificed |
|--------------|------------|
| Predictable latency under attack | Deep inspection of huge/streaming bodies |
| Offline-safe edge operation | “Live” per-request policy decisions |
| Simple global safety (version pin rollback) | Fine-grained per-POP manual tweaking |
| Stateless challenges | Provider-style CAPTCHA integrations as a dependency |

## Failure Modes

- **Bad ruleset blocks legitimate traffic**
  - Detect: block/challenge rate deltas on critical routes; origin error deltas.
  - Recover: rollback by version pin; emergency local downgrade to `LOG_ONLY` for affected routes.

- **Control plane down for minutes**
  - What happens: rollouts pause; POPs keep last-known-good indefinitely.
  - Recover: restore control plane; no edge impact required.

- **Network partition between POPs and control plane**
  - What happens: POPs keep pinned version; upgrades freeze.
  - Recover: connectivity returns; rollouts resume from known version state.

- **Event stream slow or down**
  - What happens: edge drops events once queues fill; request latency unaffected.
  - Recover: telemetry returns; counters show the gap; no edge change required.

- **CPU saturation at POP during attack**
  - Detect: rising evaluation time, queue depth, p99.
  - Recover: circuit breakers disable body inspection first; stricter limits; shift to `CHALLENGE` on high-risk routes; scale out proxy capacity.

## What We Removed

- Dedicated **Rule Distributor**: distribution is pull-by-version from the control plane’s published artifacts; POPs cache and pin.
- Separate **Analytics/Tuning** service: tuning loop runs off the event stream and feeds back into control plane rule updates.
- Any **per-request external dependency** (DB lookups, state stores, third-party challenge checks): the edge stays deterministic and offline-safe.
- Unbounded matching constructs: only bounded primitives and safe regex.

## Operational Notes

- Rule artifacts are signed; POPs verify on load. Key rotation is supported; on key compromise, POPs stop accepting new artifacts and stay pinned to last-known-good until keys are replaced.
- Telemetry is best-effort by design: bounded memory, drop-on-overload, and counters always on.
- Shadow mode is required for rule and normalization changes; promotion to `BLOCK` requires canary health gates and a tested rollback path.
```
[Pass 3/4] Refined design saved to: /Users/dominikdabrowski/pr/system-design-atlas/solutions-v2/08-security-access-control/web-application-firewall.md

[Pass 4/4] Validating Mermaid diagrams...

Generating single mermaid chart
[Pass 4/4] All Mermaid diagrams valid

==========================================
Completed (v2): Web Application Firewall (WAF)
==========================================
Draft: /Users/dominikdabrowski/pr/system-design-atlas/drafts-v2/web-application-firewall.md
Challenge: /Users/dominikdabrowski/pr/system-design-atlas/challenges/web-application-firewall.md
Final: /Users/dominikdabrowski/pr/system-design-atlas/solutions-v2/08-security-access-control/web-application-firewall.md
